---
phase: 03-ptz-control-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/ptz.ts
  - src/camera/reolink-ptz.ts
  - tests/camera/reolink-ptz.test.ts
  - tests/fixtures/reolink/get-ptz-preset.json
  - tests/fixtures/reolink/ptz-ctrl.json
autonomous: true
requirements:
  - PTZ-01
  - PTZ-02
  - PTZ-03
user_setup: []
must_haves:
  truths:
    - "PTZ motion, stop, zoom, and preset recall are defined behind one browser-safe service contract instead of raw CGI payloads leaking upward"
    - "Enabled presets are normalized from the camera response and disabled preset slots never reach the UI layer"
    - "User-facing zoom directions stay adapter-owned so later route and UI code never depend on raw `ZoomInc` or `ZoomDec` labels"
  artifacts:
    - path: "src/types/ptz.ts"
      provides: "Shared PTZ contracts for bootstrap, motion, stop, zoom, and preset recall"
      contains: "export type PtzBootstrap"
    - path: "src/camera/reolink-ptz.ts"
      provides: "RLC-423S PTZ adapter/service with capability gating, preset normalization, stop safety, and zoom mapping"
      contains: "createReolinkPtzService"
    - path: "tests/camera/reolink-ptz.test.ts"
      provides: "Fixture-backed coverage for preset parsing, motion commands, stop behavior, and preset recall payloads"
      contains: "describe(\"reolink ptz service\""
  key_links:
    - from: "src/camera/reolink-ptz.ts"
      to: "src/camera/reolink-session.ts"
      via: "all PTZ commands execute through the shared authenticated request path"
      pattern: "requestJson"
    - from: "src/camera/reolink-ptz.ts"
      to: "src/camera/capability-snapshot.ts"
      via: "capability flags gate PTZ and preset exposure before any command runs"
      pattern: "loadCapabilitySnapshot|supportsPtzControl|supportsPtzPreset"
    - from: "src/camera/reolink-ptz.ts"
      to: "src/diagnostics/debug-capture.ts"
      via: "unexpected PTZ payloads and preset failures emit sanitized artifacts instead of raw console dumps"
      pattern: "writeDebugArtifact"
---

<objective>
Create the shared PTZ contracts, the RLC-423S PTZ adapter/service, and the fixture-backed camera tests the rest of Phase 3 will build on.

Purpose: Lock the firmware-aware PTZ behavior into one backend-owned adapter before Fastify routes or React controls depend on it.
Output: Shared `src/types/ptz.ts` contracts, a tested `src/camera/reolink-ptz.ts` service, and sanitized PTZ fixtures for the known firmware.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-ptz-control-surface/03-CONTEXT.md
@.planning/phases/03-ptz-control-surface/03-RESEARCH.md
@.planning/phases/03-ptz-control-surface/03-VALIDATION.md
@src/camera/reolink-session.ts
@src/camera/capability-snapshot.ts
@src/diagnostics/debug-capture.ts
@src/types/reolink.ts

<interfaces>
From src/camera/reolink-session.ts:
```ts
export class ReolinkSession {
  async requestJson<TResponse extends readonly ReolinkApiResponse[]>(
    commands: readonly ReolinkRequest[],
  ): Promise<TResponse>;
}
```

From src/camera/capability-snapshot.ts:
```ts
export type CapabilitySnapshot = {
  supportsPtzControl: boolean;
  supportsPtzPreset: boolean;
  supportsPtzPatrol: boolean;
};

export async function loadCapabilitySnapshot(
  config: CameraConfig,
  snapshotPath?: string,
): Promise<CapabilitySnapshot>;
```

From src/types/reolink.ts:
```ts
export type ReolinkRequest = {
  cmd: ReolinkCommand;
  action?: number;
  param?: Record<string, unknown>;
};

export type ReolinkApiResponse<TValue = unknown> = {
  cmd: string;
  code: number;
  value?: TValue;
  error?: ReolinkApiError;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the PTZ contracts, preset fixtures, and bootstrap normalization path</name>
  <files>src/types/ptz.ts, src/camera/reolink-ptz.ts, tests/camera/reolink-ptz.test.ts, tests/fixtures/reolink/get-ptz-preset.json, tests/fixtures/reolink/ptz-ctrl.json</files>
  <read_first>src/types/reolink.ts, src/camera/reolink-session.ts, src/camera/capability-snapshot.ts, src/diagnostics/debug-capture.ts, tests/fixtures/reolink/get-ability.json, .planning/phases/03-ptz-control-surface/03-CONTEXT.md, .planning/phases/03-ptz-control-surface/03-RESEARCH.md, .planning/phases/03-ptz-control-surface/03-VALIDATION.md</read_first>
  <acceptance_criteria>
    - `Test-Path src/types/ptz.ts` succeeds and `Select-String -Path src/types/ptz.ts -Pattern "export type PtzBootstrap|export interface PtzService"` returns matches.
    - `Test-Path tests/fixtures/reolink/get-ptz-preset.json` and `Test-Path tests/fixtures/reolink/ptz-ctrl.json` both succeed.
    - `Select-String -Path tests/camera/reolink-ptz.test.ts -Pattern "bootstrap|preset normalization|supportsPtzPreset"` returns matches proving bootstrap and preset cases exist.
    - `npx vitest run tests/camera/reolink-ptz.test.ts --testNamePattern "bootstrap|preset"` passes.
  </acceptance_criteria>
  <action>Create `src/types/ptz.ts` with the exact shared Phase 3 contract surface: `PtzDirection = "up" | "down" | "left" | "right"`, `PtzZoomDirection = "in" | "out"`, `PtzStopReason = "release" | "explicit-stop" | "pointer-cancel" | "blur" | "visibility-hidden" | "watchdog"`, `PtzPreset`, `PtzBootstrap`, `MotionStartResult`, `MotionStopResult`, `ZoomPulseResult`, `PresetRecallResult`, and `PtzService`. In `src/camera/reolink-ptz.ts`, implement the bootstrap and preset-loading half of `createReolinkPtzService()` so it reads persisted config plus capability snapshot, returns `supportsPtzControl`, `supportsPtzPreset`, `hasVisibleStop: true` per D-06, `stopDeadlineMs: 5000`, `zoomPulseMs: 250`, and filters `GetPtzPreset` results down to enabled preset entries only per D-08. Create sanitized fixtures that include both enabled and disabled preset slots for the target firmware shape, and write fixture-backed tests proving preset normalization, empty preset handling, and the requirement that `supportsPtzPreset === false` yields an empty list without issuing `GetPtzPreset`.</action>
  <verify>
    <automated>npx vitest run tests/camera/reolink-ptz.test.ts --testNamePattern "bootstrap|preset"</automated>
  </verify>
  <done>The repo has a concrete PTZ contract file, sanitized preset fixtures, and a working bootstrap path that hides disabled presets and capability-mismatched controls before any UI or route code is added.</done>
</task>

<task type="auto">
  <name>Task 2: Implement motion, stop, zoom, and preset recall inside the adapter-owned PTZ service</name>
  <files>src/camera/reolink-ptz.ts, tests/camera/reolink-ptz.test.ts</files>
  <read_first>src/camera/reolink-ptz.ts, src/types/ptz.ts, src/camera/reolink-session.ts, src/diagnostics/debug-capture.ts, tests/camera/reolink-ptz.test.ts, tests/fixtures/reolink/get-ptz-preset.json, tests/fixtures/reolink/ptz-ctrl.json, .planning/phases/03-ptz-control-surface/03-RESEARCH.md</read_first>
  <acceptance_criteria>
    - `Select-String -Path src/camera/reolink-ptz.ts -Pattern "ToPos|ZoomInc|ZoomDec|Stop"` returns matches so preset recall, adapter-owned zoom mapping, and stop commands are all encoded in one file.
    - `Select-String -Path src/camera/reolink-ptz.ts -Pattern "5000|250|writeDebugArtifact"` returns matches showing the stop watchdog, bounded zoom pulse, and debug capture hook are present.
    - `Select-String -Path tests/camera/reolink-ptz.test.ts -Pattern "watchdog|idempotent stop|ToPos"` returns matches covering stop safety and preset recall.
    - `npx vitest run tests/camera/reolink-ptz.test.ts` passes.
  </acceptance_criteria>
  <action>Finish `createReolinkPtzService()` so `startMotion()` sends exactly one `PtzCtrl` start command per call for `up`, `down`, `left`, or `right`; `stopMotion()` sends `op: "Stop"` and is idempotent when no motion is active; `pulseZoom()` converts user-facing `in` and `out` actions into adapter-owned raw operations with a `250ms` bounded pulse plus automatic stop; and `recallPreset()` issues `PtzCtrl` with `op: "ToPos"` and the preset `id` field rather than `index`, per the research findings. Keep the raw zoom op mapping private to `src/camera/reolink-ptz.ts` so later UI labels never depend on firmware-specific names. Add an injectable `5000ms` watchdog timer that auto-stops stale motion as a backend safety net for PTZ-02, and capture sanitized debug artifacts whenever malformed preset payloads or rejected PTZ responses prevent normalization or recall.</action>
  <verify>
    <automated>npx vitest run tests/camera/reolink-ptz.test.ts</automated>
  </verify>
  <done>The backend has a single tested PTZ adapter/service that owns motion, stop, zoom, preset recall, zoom mapping, and fallback-safe diagnostics for the RLC-423S.</done>
</task>

</tasks>

<verification>
- [ ] `npx vitest run tests/camera/reolink-ptz.test.ts` succeeds
- [ ] `src/types/ptz.ts` exports the shared PTZ DTOs and `PtzService` interface
- [ ] `src/camera/reolink-ptz.ts` is the only place where raw PTZ op names such as `Left`, `Right`, `Stop`, `ZoomInc`, `ZoomDec`, and `ToPos` appear
</verification>

<success_criteria>
- Phase 3 has a concrete PTZ contract for later plans to consume
- Preset normalization respects capability gating and hides disabled slots
- Stop safety and zoom mapping are owned by the adapter layer instead of duplicated in routes or React
</success_criteria>

<output>
After completion, create `.planning/phases/03-ptz-control-surface/03-01-SUMMARY.md`
</output>
