---
phase: 03-ptz-control-surface
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - web/src/App.tsx
  - web/src/styles.css
  - web/src/lib/ptz-api.ts
  - web/src/hooks/use-ptz-controls.ts
  - web/src/components/PtzPanel.tsx
  - web/src/components/PtzPresetGrid.tsx
  - tests/web/ptz-controls.test.tsx
autonomous: true
requirements:
  - PTZ-01
  - PTZ-02
  - PTZ-03
user_setup: []
must_haves:
  truths:
    - "The dashboard shows an attached PTZ panel beside the viewer on wide layouts and below it on narrow layouts per the approved UI contract"
    - "Pan and tilt are hold-to-move actions that stop on release, explicit Stop, pointer cancellation, blur, and hidden-tab transitions"
    - "Preset controls appear only when supported and render as a visible list/grid instead of a hidden disclosure"
  artifacts:
    - path: "web/src/hooks/use-ptz-controls.ts"
      provides: "Browser PTZ state machine for bootstrap, hold-to-move, stop safety, zoom pulses, and preset recall"
      contains: "visibilitychange"
    - path: "web/src/components/PtzPanel.tsx"
      provides: "Attached PTZ panel with motion, zoom, stop, and preset surfaces"
      contains: "Stop Camera"
    - path: "tests/web/ptz-controls.test.tsx"
      provides: "jsdom coverage for PTZ interaction lifecycle and UI gating"
      contains: "lostpointercapture"
  key_links:
    - from: "web/src/hooks/use-ptz-controls.ts"
      to: "web/src/lib/ptz-api.ts"
      via: "all browser PTZ actions use the Fastify PTZ API instead of camera CGI calls"
      pattern: "/api/ptz"
    - from: "web/src/App.tsx"
      to: "web/src/components/PtzPanel.tsx"
      via: "the viewer dashboard mounts the PTZ panel in the attached cluster above mode switching and diagnostics"
      pattern: "PtzPanel"
    - from: "web/src/components/PtzPanel.tsx"
      to: "web/src/hooks/use-ptz-controls.ts"
      via: "pointer lifecycle and preset button states are driven entirely by the PTZ hook"
      pattern: "beginMotion|stopMotion|recallPreset"
---

<objective>
Build the browser PTZ hook, API client, attached PTZ panel, and interaction tests that turn the backend PTZ service into the approved viewer-adjacent control surface.

Purpose: Deliver the actual Phase 3 user experience without breaking the existing viewer-first shell from Phase 2.
Output: PTZ browser API client, `usePtzControls()` hook, viewer-attached PTZ panel and preset grid, updated dashboard layout/CSS, and jsdom coverage for motion and preset interactions.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-ptz-control-surface/03-CONTEXT.md
@.planning/phases/03-ptz-control-surface/03-RESEARCH.md
@.planning/phases/03-ptz-control-surface/03-UI-SPEC.md
@.planning/phases/03-ptz-control-surface/03-VALIDATION.md
@.planning/phases/02-browser-live-view-pipeline/02-04-SUMMARY.md
@web/src/App.tsx
@web/src/styles.css
@web/src/hooks/use-live-view.ts

<interfaces>
Existing viewer contract from web/src/hooks/use-live-view.ts:
```ts
export function useLiveView(): {
  state: { kind: ViewerStateKind; reason?: string };
  currentModeId: LiveModeId | null;
  nextFallbackModeId: LiveModeId | null;
  renderKind: "video" | "image";
  activePlayback: LiveViewPlayback | null;
  bindVideoElement(node: HTMLVideoElement | null): void;
  bindImageElement(node: HTMLImageElement | null): void;
  retry(): Promise<void>;
  selectMode(modeId: LiveModeId): Promise<void>;
};
```

PTZ HTTP contract from plans 03-01 and 03-02:
```ts
GET /api/ptz -> PtzBootstrap
POST /api/ptz/motion/start body { direction: "up" | "down" | "left" | "right" }
POST /api/ptz/stop body { reason?: PtzStopReason }
POST /api/ptz/zoom body { direction: "in" | "out" }
POST /api/ptz/presets/:presetId/recall -> PresetRecallResult
```

Exact UI copy from 03-UI-SPEC:
```text
Camera Control
PTZ Control
Hold to move. Release to stop.
Tap for a short zoom step.
Presets
Stop Camera
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the browser PTZ API client and hook-driven stop lifecycle</name>
  <files>web/src/lib/ptz-api.ts, web/src/hooks/use-ptz-controls.ts, tests/web/ptz-controls.test.tsx</files>
  <read_first>web/src/hooks/use-live-view.ts, web/src/App.tsx, src/types/ptz.ts, web/src/lib/live-view-api.ts, tests/web/live-view-controls.test.tsx, .planning/phases/03-ptz-control-surface/03-RESEARCH.md, .planning/phases/03-ptz-control-surface/03-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - `Test-Path web/src/lib/ptz-api.ts` and `Test-Path web/src/hooks/use-ptz-controls.ts` both succeed.
    - `Select-String -Path web/src/lib/ptz-api.ts -Pattern "/api/ptz|motion/start|/zoom|/stop"` returns matches for the full browser API surface.
    - `Select-String -Path web/src/hooks/use-ptz-controls.ts -Pattern "setPointerCapture|pointercancel|lostpointercapture|visibilitychange|blur"` returns matches for the required stop-safety lifecycle from PTZ-02 and D-05/D-06.
    - `npx vitest run tests/web/ptz-controls.test.tsx --testNamePattern "motion|stop|zoom"` passes.
  </acceptance_criteria>
  <action>Create `web/src/lib/ptz-api.ts` as the single browser client for the Fastify PTZ endpoints from plan 03-02. Implement `usePtzControls()` in `web/src/hooks/use-ptz-controls.ts` so it fetches PTZ bootstrap on mount, exposes normalized state for `supportsPtzControl`, `supportsPtzPreset`, `presets`, `activeDirection`, `busyAction`, and short status copy, and enforces the Phase 3 interaction rules: press-and-hold motion for D-01, discrete click zoom for D-02, stop on `pointerup`, `pointercancel`, `lostpointercapture`, `window.blur`, and `document.visibilitychange === "hidden"` for D-05, and an explicit Stop path for D-06. Send exactly one motion-start request per hold, never spam repeated move commands while held, and disable conflicting directional buttons while motion is active. Use `tests/web/ptz-controls.test.tsx` to cover the pointer lifecycle, stop fallbacks, and click-based zoom behavior with mocked PTZ API calls.</action>
  <verify>
    <automated>npx vitest run tests/web/ptz-controls.test.tsx --testNamePattern "motion|stop|zoom"</automated>
  </verify>
  <done>The browser layer has a dedicated PTZ hook and API client that enforce hold-to-move, click-to-zoom, and stop-on-release semantics before any visual panel work is added.</done>
</task>

<task type="auto">
  <name>Task 2: Attach the PTZ panel to the viewer layout and render visible preset controls</name>
  <files>web/src/App.tsx, web/src/styles.css, web/src/components/PtzPanel.tsx, web/src/components/PtzPresetGrid.tsx, tests/web/ptz-controls.test.tsx</files>
  <read_first>web/src/App.tsx, web/src/styles.css, web/src/components/ModeSwitcher.tsx, web/src/components/DiagnosticsDisclosure.tsx, web/src/hooks/use-ptz-controls.ts, tests/web/ptz-controls.test.tsx, .planning/phases/03-ptz-control-surface/03-UI-SPEC.md, .planning/phases/02-browser-live-view-pipeline/02-04-SUMMARY.md</read_first>
  <acceptance_criteria>
    - `Select-String -Path web/src/App.tsx -Pattern "PtzPanel|viewer-ptz-cluster"` returns matches showing the PTZ panel is attached to the viewer layout ahead of mode switching and diagnostics.
    - `Select-String -Path web/src/components/PtzPanel.tsx -Pattern "Camera Control|PTZ Control|Stop Camera|Hold to move\\. Release to stop\\.|Tap for a short zoom step\\."` returns matches for the required UI copy.
    - `Select-String -Path web/src/styles.css -Pattern "980px|viewer-ptz-cluster|ptz-panel|ptz-presets"` returns matches for the two-column/stacked layout and PTZ styling hooks.
    - `npx vitest run tests/web/ptz-controls.test.tsx` passes.
    - `npm run build:web` passes.
  </acceptance_criteria>
  <action>Build `web/src/components/PtzPanel.tsx` and `web/src/components/PtzPresetGrid.tsx` using the approved Phase 3 UI contract. In `web/src/App.tsx`, wrap the existing `LiveViewerFrame` and the new `PtzPanel` inside a `viewer-ptz-cluster` so the layout is two columns at `980px` and wider and stacks the PTZ panel directly below the viewer on narrower widths per D-03 and D-04. Keep `ModeSwitcher` and `DiagnosticsDisclosure` below that cluster. Use exact copy from the UI spec, keep `Stop Camera` always visible, render presets as a visible two-column grid when `supportsPtzPreset` is true per D-07/D-08, hide the entire presets section when presets are unsupported, and show the unsupported PTZ message `PTZ is not available for this camera profile.` when `supportsPtzControl` is false. Update `tests/web/ptz-controls.test.tsx` so it verifies visible Stop, preset gating, preset disabling while recall is in flight, and the presence of the approved helper copy and layout wiring.</action>
  <verify>
    <automated>npm run build:web && npx vitest run tests/web/ptz-controls.test.tsx</automated>
  </verify>
  <done>The dashboard now presents a viewer-attached PTZ panel that matches the approved layout, copy, and interaction rules while preserving the Phase 2 viewer shell hierarchy.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build:web` succeeds
- [ ] `npx vitest run tests/web/ptz-controls.test.tsx` succeeds
- [ ] With the local camera reachable, hold each direction for 1-2 seconds and confirm motion stops promptly on release, explicit `Stop Camera`, window blur, and hidden-tab transitions
- [ ] With the target firmware `v2.0.0.1055_17110905_v1.0.0.30`, tap `Zoom In` and `Zoom Out` once each and confirm the physical lens direction matches the label; if reversed, only adjust the private zoom-op mapping in `src/camera/reolink-ptz.ts`
- [ ] If at least one preset is enabled on the camera, trigger it from the visible preset grid and confirm the camera lands on the saved position
</verification>

<success_criteria>
- The viewer dashboard exposes PTZ controls without floating them over the video
- Hold-to-move and stop recovery behavior are test-covered and hardware-verifiable
- Preset controls remain simple, visible, and capability-gated for the target camera profile
</success_criteria>

<output>
After completion, create `.planning/phases/03-ptz-control-surface/03-03-SUMMARY.md`
</output>
