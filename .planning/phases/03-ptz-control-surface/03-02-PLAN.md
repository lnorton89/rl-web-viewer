---
phase: 03-ptz-control-surface
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/server/routes/ptz.ts
  - src/server/create-server.ts
  - tests/server/ptz-routes.test.ts
autonomous: true
requirements:
  - PTZ-01
  - PTZ-02
  - PTZ-03
user_setup: []
must_haves:
  truths:
    - "The browser can load PTZ capability and preset state through a local app endpoint without seeing camera credentials or raw CGI payloads"
    - "Pan, tilt, stop, zoom, and preset recall routes reject unsupported actions from the capability snapshot instead of surfacing dead controls"
    - "The Fastify app mounts PTZ routes beside the existing live-view routes so the dashboard can control PTZ through the same local control plane"
  artifacts:
    - path: "src/server/routes/ptz.ts"
      provides: "Browser-safe PTZ bootstrap and command endpoints"
      contains: "/api/ptz/motion/start"
    - path: "src/server/create-server.ts"
      provides: "Fastify registration for the PTZ route plugin"
      contains: "ptzRoutes"
    - path: "tests/server/ptz-routes.test.ts"
      provides: "Route coverage for PTZ bootstrap, command routing, capability gating, and invalid payload handling"
      contains: "describe(\"ptz routes\""
  key_links:
    - from: "src/server/create-server.ts"
      to: "src/server/routes/ptz.ts"
      via: "the app server mounts the PTZ route plugin for browser clients"
      pattern: "register\\(ptzRoutes"
    - from: "src/server/routes/ptz.ts"
      to: "src/camera/reolink-ptz.ts"
      via: "all control endpoints delegate to the backend-owned PTZ service instead of hand-building commands"
      pattern: "createReolinkPtzService|PtzService"
    - from: "src/server/routes/ptz.ts"
      to: "src/types/ptz.ts"
      via: "request and response payloads use shared PTZ DTOs"
      pattern: "PtzBootstrap|PtzDirection|PtzZoomDirection"
---

<objective>
Expose the PTZ service from plan 03-01 through browser-safe Fastify routes and wire the plugin into the existing local app server.

Purpose: Keep the dashboard on the established Fastify control plane so the browser never talks to the camera directly.
Output: `ptzRoutes`, updated server registration, and integration tests for PTZ bootstrap, motion, stop, zoom, and preset recall endpoints.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-ptz-control-surface/03-CONTEXT.md
@.planning/phases/03-ptz-control-surface/03-RESEARCH.md
@.planning/phases/03-ptz-control-surface/03-VALIDATION.md
@.planning/phases/02-browser-live-view-pipeline/02-02-SUMMARY.md
@src/server/create-server.ts
@src/server/routes/live-view.ts

<interfaces>
From plan 03-01 (`src/types/ptz.ts` and `src/camera/reolink-ptz.ts`):
```ts
export type PtzDirection = "up" | "down" | "left" | "right";
export type PtzZoomDirection = "in" | "out";
export type PtzBootstrap = {
  supportsPtzControl: boolean;
  supportsPtzPreset: boolean;
  hasVisibleStop: true;
  stopDeadlineMs: number;
  zoomPulseMs: number;
  presets: PtzPreset[];
};

export interface PtzService {
  getBootstrap(): Promise<PtzBootstrap>;
  startMotion(direction: PtzDirection): Promise<MotionStartResult>;
  stopMotion(reason?: PtzStopReason): Promise<MotionStopResult>;
  pulseZoom(direction: PtzZoomDirection): Promise<ZoomPulseResult>;
  recallPreset(presetId: number): Promise<PresetRecallResult>;
}
```

Existing Fastify pattern from src/server/routes/live-view.ts:
```ts
export type LiveViewRouteDependencies = {
  buildLiveViewBootstrap?: (requestHost: string) => Promise<LiveViewBootstrap>;
};

export const liveViewRoutes: FastifyPluginAsync<LiveViewRouteDependencies> = async (
  app,
  options,
) => { /* register routes here */ };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the PTZ route plugin with browser-safe DTOs and capability-gated command endpoints</name>
  <files>src/server/routes/ptz.ts, tests/server/ptz-routes.test.ts</files>
  <read_first>src/server/routes/live-view.ts, src/server/create-server.ts, src/types/ptz.ts, src/camera/reolink-ptz.ts, tests/server/live-view-routes.test.ts, .planning/phases/03-ptz-control-surface/03-RESEARCH.md, .planning/phases/03-ptz-control-surface/03-VALIDATION.md</read_first>
  <acceptance_criteria>
    - `Test-Path src/server/routes/ptz.ts` succeeds and `Select-String -Path src/server/routes/ptz.ts -Pattern "/api/ptz|motion/start|/zoom|presetId"` returns matches for all required endpoints.
    - `Select-String -Path src/server/routes/ptz.ts -Pattern "409|supportsPtzControl|supportsPtzPreset"` returns matches showing capability gating is enforced in the route layer.
    - `Select-String -Path tests/server/ptz-routes.test.ts -Pattern "returns ptz bootstrap|rejects invalid|unsupported"` returns matches for bootstrap, validation, and capability-gate cases.
    - `npx vitest run tests/server/ptz-routes.test.ts --testNamePattern "bootstrap|invalid|unsupported"` passes.
  </acceptance_criteria>
  <action>Create `src/server/routes/ptz.ts` as a Fastify plugin that mirrors the established live-view route pattern. Export `PtzRouteDependencies = { createPtzService?: () => Promise<PtzService> | PtzService }` so tests can inject a fake service. Implement the exact browser API surface for Phase 3: `GET /api/ptz` returns `PtzBootstrap`; `POST /api/ptz/motion/start` accepts `{ direction: "up" | "down" | "left" | "right" }`; `POST /api/ptz/stop` accepts `{ reason?: PtzStopReason }`; `POST /api/ptz/zoom` accepts `{ direction: "in" | "out" }`; and `POST /api/ptz/presets/:presetId/recall` recalls one saved preset. Validate request bodies with `zod`, return `400` for malformed directions or preset ids, return `409` with exact JSON `{ error: "PTZ control is not available for this camera profile." }` or `{ error: "PTZ presets are not available for this camera profile." }` when capability flags block the action, and keep every response browser-safe by returning only normalized DTOs and status strings from the PTZ service.</action>
  <verify>
    <automated>npx vitest run tests/server/ptz-routes.test.ts --testNamePattern "bootstrap|invalid|unsupported"</automated>
  </verify>
  <done>The repo has a PTZ Fastify route plugin with explicit request contracts, capability gating, and targeted integration coverage for bootstrap and rejection behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Mount the PTZ routes in the app server and finish end-to-end server coverage</name>
  <files>src/server/create-server.ts, src/server/routes/ptz.ts, tests/server/ptz-routes.test.ts</files>
  <read_first>src/server/create-server.ts, src/server/routes/ptz.ts, tests/server/ptz-routes.test.ts, src/server/routes/live-view.ts, .planning/phases/02-browser-live-view-pipeline/02-02-SUMMARY.md</read_first>
  <acceptance_criteria>
    - `Select-String -Path src/server/create-server.ts -Pattern "ptzRoutes|ptz\\?:"` returns matches showing the server factory accepts PTZ dependencies and registers the plugin.
    - `Select-String -Path tests/server/ptz-routes.test.ts -Pattern "createServer|/api/ptz/motion/start|/api/live-view"` returns matches proving the mounted server surface is exercised.
    - `npx vitest run tests/server/ptz-routes.test.ts` passes.
    - `npm run build:server` passes.
  </acceptance_criteria>
  <action>Extend `createServer()` to accept a new `ptz?: PtzRouteDependencies` option and register `ptzRoutes` after `liveViewRoutes` so the browser gets PTZ control from the same local app server. Finish `tests/server/ptz-routes.test.ts` by instantiating `createServer({ staticRoot, ptz: { createPtzService: ... }, liveView: ... })`, then assert that mounted routes call the injected PTZ service methods, preserve existing live-view behavior, and return `202` for motion start, zoom pulse, and preset recall while returning `200` for bootstrap and stop. Keep the server surface LAN-local, credential-safe, and aligned with the shared PTZ DTOs from plan 03-01.</action>
  <verify>
    <automated>npm run build:server && npx vitest run tests/server/ptz-routes.test.ts</automated>
  </verify>
  <done>The Fastify app now exposes a complete PTZ API surface that later browser code can consume without knowing anything about camera auth, CGI routes, or firmware-specific command names.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build:server` succeeds
- [ ] `npx vitest run tests/server/ptz-routes.test.ts` succeeds
- [ ] `/api/ptz`, `/api/ptz/motion/start`, `/api/ptz/stop`, `/api/ptz/zoom`, and `/api/ptz/presets/:presetId/recall` are mounted through `createServer()`
</verification>

<success_criteria>
- The browser has one Fastify-owned PTZ API surface to call
- Route validation and capability gating stop unsupported PTZ behavior before it reaches the camera
- Existing live-view server behavior remains intact while PTZ routes are added
</success_criteria>

<output>
After completion, create `.planning/phases/03-ptz-control-surface/03-02-SUMMARY.md`
</output>
