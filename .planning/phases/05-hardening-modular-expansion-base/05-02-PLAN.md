---
phase: 05-hardening-modular-expansion-base
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - src/diagnostics/debug-capture.ts
  - src/diagnostics/operation-diagnostics.ts
  - src/server/plugins/diagnostics.ts
  - src/server/create-server.ts
  - src/media/live-view-service.ts
  - src/camera/reolink-session.ts
  - src/server/routes/live-view.ts
  - tests/server/diagnostics-logging.test.ts
  - tests/media/live-view-repeatability.test.ts
  - tests/server/dashboard-bootstrap-repeatability.test.ts
  - tests/server/live-view-routes.test.ts
autonomous: true
requirements:
  - PH5-SC2
  - PH5-SC3
must_haves:
  truths:
    - "Repeated local backend operations produce structured, sanitized logs that identify the adapter, operation scope, and artifact path instead of forcing raw dump inspection per D-05 and D-06."
    - "Repeated dashboard bootstrap, camera connect/session entry, and live-view relay startup hold up across restart and failure loops without requiring process restarts per D-01, D-02, and D-07."
    - "The backend keeps diagnostics secondary in browser payloads while still giving operators actionable failure summaries and log correlation per D-05 and D-08."
  artifacts:
    - path: "src/diagnostics/operation-diagnostics.ts"
      provides: "The shared runtime diagnostics helper and sanitized log bindings"
      contains: "createOperationDiagnostics"
    - path: "src/server/plugins/diagnostics.ts"
      provides: "Fastify plugin wiring for request-scoped diagnostics"
      contains: "diagnosticsPlugin"
    - path: "tests/server/diagnostics-logging.test.ts"
      provides: "Coverage for request correlation, redaction, and artifact linkage"
      contains: "describe(\"diagnostics logging\""
    - path: "tests/media/live-view-repeatability.test.ts"
      provides: "Coverage for relay start, repeat start, failure, and recovery loops"
      contains: "describe(\"live view repeatability\""
    - path: "tests/server/dashboard-bootstrap-repeatability.test.ts"
      provides: "Coverage for repeated dashboard bootstrap/connect/session re-entry behavior through the local server seam"
      contains: "describe(\"dashboard bootstrap repeatability\""
  key_links:
    - from: "src/server/create-server.ts"
      to: "src/server/plugins/diagnostics.ts"
      via: "the app server registers one diagnostics plugin for all routes"
      pattern: "diagnosticsPlugin"
    - from: "src/media/live-view-service.ts"
      to: "src/camera/adapters/index.ts"
      via: "live-view bootstrap and relay setup resolve the adapter instead of parsing model quirks ad hoc"
      pattern: "resolveCameraAdapter"
    - from: "src/server/routes/live-view.ts"
      to: "src/camera/reolink-session.ts"
      via: "dashboard bootstrap and repeated route entry reuse one recoverable session path instead of forcing a process restart"
      pattern: "createReolinkSession|login|execute"
    - from: "src/diagnostics/operation-diagnostics.ts"
      to: "src/diagnostics/debug-capture.ts"
      via: "logs link to sanitized artifacts when runtime failures warrant deeper inspection"
      pattern: "writeDebugArtifact"
---

<objective>
Centralize backend diagnostics, add repeatability coverage for live-view/runtime loops, and keep browser-facing diagnostics short and safe.

Purpose: Satisfy D-01, D-02, D-05, D-06, and D-09 by making the control plane easier to trust and easier to debug under repeated local use.
Output: Shared diagnostics helpers, Fastify diagnostics wiring, repeated bootstrap/connect/session recovery hardening, and regression tests for logs plus retry/restart behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-hardening-modular-expansion-base/05-CONTEXT.md
@.planning/phases/05-hardening-modular-expansion-base/05-RESEARCH.md
@.planning/phases/05-hardening-modular-expansion-base/05-VALIDATION.md
@.planning/phases/05-hardening-modular-expansion-base/05-01-PLAN.md
@src/index.ts
@src/server/create-server.ts
@src/media/live-view-service.ts
@src/diagnostics/debug-capture.ts
@src/server/routes/live-view.ts
@src/camera/reolink-session.ts

<interfaces>
From plan 05-01:
```ts
export interface CameraAdapter {
  adapterId: string;
  classifyFailure(scope: "probe" | "live-view" | "ptz" | "settings", error: unknown): string;
  resolveLiveStreams(config: CameraConfig, snapshot: CapabilitySnapshot): ResolvedReolinkLiveStreams;
}
```

Current live-view bootstrap and relay seam from src/media/live-view-service.ts:
```ts
export async function buildLiveViewBootstrap(requestHost: string): Promise<LiveViewBootstrap>;
export async function startMediaRelay(): Promise<void>;
export function getMediaRelayHealth(): MediaRelayHealth;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared structured diagnostics and request-scoped logging</name>
  <files>src/diagnostics/operation-diagnostics.ts, src/diagnostics/debug-capture.ts, src/server/plugins/diagnostics.ts, src/server/create-server.ts, tests/server/diagnostics-logging.test.ts</files>
  <read_first>src/diagnostics/debug-capture.ts, src/server/create-server.ts, src/camera/adapters/camera-adapter.ts, tests/debug-capture.test.ts, .planning/phases/05-hardening-modular-expansion-base/05-RESEARCH.md, .planning/phases/05-hardening-modular-expansion-base/05-VALIDATION.md</read_first>
  <acceptance_criteria>
    - `Test-Path src/diagnostics/operation-diagnostics.ts`, `Test-Path src/server/plugins/diagnostics.ts`, and `Test-Path tests/server/diagnostics-logging.test.ts` all succeed.
    - `Select-String -Path src/diagnostics/operation-diagnostics.ts -Pattern "createOperationDiagnostics|adapterId|scope|cameraHost|artifactPath|requestId"` returns matches.
    - `Select-String -Path src/server/create-server.ts -Pattern "diagnosticsPlugin|logger|requestId"` returns matches.
    - `npx vitest run tests/server/diagnostics-logging.test.ts tests/debug-capture.test.ts --project node` passes.
  </acceptance_criteria>
  <behavior>
    - Test 1: Logs include `requestId`, `adapterId`, `scope`, and `cameraHost` for server and session operations without leaking username, password, token, or raw RTSP URLs.
    - Test 2: When a debug artifact is written, the log payload includes the artifact path and a short classified message instead of dumping raw protocol payloads per D-05 and D-06.
    - Test 3: Fastify server code shares one diagnostics helper for route logging and artifact correlation instead of route-local logging variations.
  </behavior>
  <action>Create `src/diagnostics/operation-diagnostics.ts` exporting `createOperationDiagnostics()` and `createDiagnosticsLoggerBindings()` that produce sanitized bindings with the exact keys `requestId`, `adapterId`, `scope`, `cameraHost`, `outcome`, and optional `artifactPath`. Extend `src/diagnostics/debug-capture.ts` so `writeDebugArtifact()` still sanitizes request and response bodies but now also returns a path suitable for log linkage and preserves a stable `timestamp` field. Add `src/server/plugins/diagnostics.ts` as a Fastify plugin that decorates the app with a diagnostics factory using Fastify request ids; register it from `createServer()` so all routes can create child loggers without reimplementing redaction. Keep this task limited to diagnostics infrastructure and route/plugin wiring, not live-view/session repeatability logic. Do not log `config.username`, `config.password`, `token.name`, `token=`, or `rtsp://` anywhere; keep those values redacted or omitted per D-06.</action>
  <verify>
    <automated>npx vitest run tests/server/diagnostics-logging.test.ts tests/debug-capture.test.ts --project node</automated>
  </verify>
  <done>The backend has one structured diagnostics path with redaction, correlation ids, and artifact linkage shared across startup, routes, and session work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden repeated dashboard bootstrap, camera connect/session re-entry, and live-view relay recovery</name>
  <files>src/media/live-view-service.ts, src/server/routes/live-view.ts, src/camera/reolink-session.ts, tests/media/live-view-repeatability.test.ts, tests/server/dashboard-bootstrap-repeatability.test.ts, tests/server/live-view-routes.test.ts</files>
  <read_first>src/media/live-view-service.ts, src/server/routes/live-view.ts, src/camera/reolink-session.ts, src/camera/adapters/index.ts, src/camera/adapters/reolink-rlc-423s-adapter.ts, tests/media/live-view-errors.test.ts, tests/server/live-view-routes.test.ts, .planning/phases/05-hardening-modular-expansion-base/05-RESEARCH.md, .planning/phases/05-hardening-modular-expansion-base/05-CONTEXT.md</read_first>
  <acceptance_criteria>
    - `Test-Path tests/media/live-view-repeatability.test.ts` succeeds.
    - `Test-Path tests/server/dashboard-bootstrap-repeatability.test.ts` succeeds.
    - `Select-String -Path src/media/live-view-service.ts -Pattern "resolveCameraAdapter|relayHealth|starting|ready|failed|artifactPath|adapterId"` returns matches.
    - `Select-String -Path src/server/routes/live-view.ts -Pattern "reason|requestId|relay|artifactPath|bootstrap"` returns matches showing browser-safe diagnostics shaping for repeated entry.
    - `Select-String -Path src/camera/reolink-session.ts -Pattern "child\\(|adapterId|scope|session-login|session-request"` returns matches showing session re-entry logging uses the shared helper.
    - `Select-String -Path tests/media/live-view-repeatability.test.ts -Pattern "repeat start|restart|recovery|unsupported"` returns matches.
    - `Select-String -Path tests/server/dashboard-bootstrap-repeatability.test.ts -Pattern "bootstrap|repeat|session|re-entry|no restart"` returns matches.
    - `npx vitest run tests/media/live-view-repeatability.test.ts tests/server/dashboard-bootstrap-repeatability.test.ts tests/server/live-view-routes.test.ts --project node` passes.
  </acceptance_criteria>
  <behavior>
    - Test 1: `startMediaRelay()` is idempotent under repeated calls and does not spawn duplicate relay state machines for the same process loop per D-07.
    - Test 2: `buildLiveViewBootstrap()` resolves the adapter from the persisted identity and uses adapter-owned failure classification instead of hardcoded RLC-423S parsing per D-03 and D-04.
    - Test 3: Repeated dashboard bootstrap and camera connect/session re-entry recover through the local server seam without requiring a Node process restart, preserving short reason text plus correlation metadata per D-01 and D-02.
    - Test 4: Browser responses keep diagnostics secondary by exposing only short reason text, request correlation, and optional artifact references, never raw protocol payloads or secrets per D-05 and D-06.
  </behavior>
  <action>Update `src/media/live-view-service.ts` to resolve the concrete adapter from the persisted capability snapshot identity via `resolveCameraAdapter()`, then call the adapter for stream resolution and failure classification instead of hardcoded `resolveReolinkLiveStreams()` and ad hoc message parsing. Keep the existing `relayHealth` state machine, but make `startMediaRelay()` explicitly idempotent when called while the relay is already `starting` or `ready`, and make failure transitions preserve the latest classified reason plus optional `artifactPath`. Extend `buildLiveViewBootstrap()` so its diagnostics object remains browser-safe but now includes `requestId: string | null` and `artifactPath: string | null` fields when available; the route layer must pass those through only as short references, not as raw file contents. Update `src/camera/reolink-session.ts` so repeated login/request paths can recover from expired or failed sessions on the next dashboard bootstrap/connect attempt instead of leaving the process stuck until restart. Update `src/server/routes/live-view.ts` to keep `/api/live-view` and `/api/live-view/health` browser-safe while surfacing the short classified reason, relay state, and correlation metadata from the service. Add `tests/media/live-view-repeatability.test.ts` covering repeated `startMediaRelay()` calls, restart-after-failure, unsupported adapter cases, and preserved failure reasons. Add `tests/server/dashboard-bootstrap-repeatability.test.ts` covering repeated dashboard bootstrap/connect/session re-entry through the local API seam, including failure-then-recovery without process restart. Extend route tests so `/api/live-view` still rejects unsafe payloads even with the new diagnostics fields. This task is the concrete repeated-use hardening task required by D-02.</action>
  <verify>
    <automated>npx vitest run tests/media/live-view-repeatability.test.ts tests/server/dashboard-bootstrap-repeatability.test.ts tests/server/live-view-routes.test.ts --project node</automated>
  </verify>
  <done>The local backend now survives repeated dashboard bootstrap/connect/session/live-view entry loops more cleanly, with automated proof that recovery works without a process restart.</done>
</task>

</tasks>

<verification>
- [ ] `npx vitest run tests/server/diagnostics-logging.test.ts tests/debug-capture.test.ts --project node` succeeds
- [ ] `npx vitest run tests/media/live-view-repeatability.test.ts tests/server/dashboard-bootstrap-repeatability.test.ts tests/server/live-view-routes.test.ts --project node` succeeds
- [ ] `src/server/create-server.ts` registers one diagnostics plugin before route plugins
</verification>

<success_criteria>
- The backend exposes structured, sanitized observability instead of fragmented logs
- Repeated dashboard bootstrap/connect/session/live-view recovery is covered by targeted tests
- Browser-visible diagnostics stay short and safe while still pointing to deeper evidence when needed
</success_criteria>

<output>
After completion, create `.planning/phases/05-hardening-modular-expansion-base/05-02-SUMMARY.md`
</output>
