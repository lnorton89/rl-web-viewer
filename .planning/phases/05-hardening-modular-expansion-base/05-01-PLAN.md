---
phase: 05-hardening-modular-expansion-base
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/camera/adapters/camera-adapter.ts
  - src/camera/adapters/reolink-rlc-423s-adapter.ts
  - src/camera/adapters/index.ts
  - src/camera/reolink-discovery.ts
  - src/camera/reolink-live-streams.ts
  - src/camera/reolink-ptz.ts
  - src/camera/reolink-settings.ts
  - tests/camera/adapter-contract.test.ts
  - docs/camera-adapters.md
autonomous: true
requirements:
  - PH5-SC1
  - PH5-SC4
must_haves:
  truths:
    - "The codebase exposes one explicit adapter contract that owns discovery, capability mapping, live-stream resolution, PTZ, settings, and diagnostics classification per D-03 and D-10."
    - "RLC-423S-specific behavior stays behind adapter-owned modules and tests instead of leaking farther into routes, hooks, or shared browser contracts per D-04."
    - "A future model phase can add another adapter by implementing one documented interface instead of restructuring the current camera stack."
  artifacts:
    - path: "src/camera/adapters/camera-adapter.ts"
      provides: "The shared camera adapter interface, adapter deps, and registry-facing types"
      contains: "export interface CameraAdapter"
    - path: "src/camera/adapters/reolink-rlc-423s-adapter.ts"
      provides: "The Phase 5 RLC-423S adapter implementation"
      contains: "createRlc423sAdapter"
    - path: "tests/camera/adapter-contract.test.ts"
      provides: "Contract coverage proving one adapter object owns the existing RLC-423S seams"
      contains: "describe(\"camera adapter contract\""
    - path: "docs/camera-adapters.md"
      provides: "Human-readable extension seam documentation for future model phases"
      contains: "CameraAdapter"
  key_links:
    - from: "src/camera/adapters/reolink-rlc-423s-adapter.ts"
      to: "src/camera/reolink-discovery.ts"
      via: "the adapter owns camera probing and identity matching"
      pattern: "probeCamera|getDevInfo|getAbility"
    - from: "src/camera/adapters/reolink-rlc-423s-adapter.ts"
      to: "src/camera/reolink-live-streams.ts"
      via: "the adapter owns firmware-specific stream resolution"
      pattern: "resolveReolinkLiveStreams"
    - from: "src/camera/adapters/reolink-rlc-423s-adapter.ts"
      to: "src/camera/reolink-ptz.ts|src/camera/reolink-settings.ts"
      via: "the adapter owns PTZ and settings service creation"
      pattern: "createReolinkPtzService|createReolinkSettingsService"
---

<objective>
Define the explicit adapter contract, wrap the current RLC-423S seams behind it, and document how future model adapters plug into the same boundary.

Purpose: Satisfy D-03, D-04, and D-10 by ending v1 with a real extension seam instead of a pile of `reolink-*` entrypoints that future phases must untangle.
Output: One `CameraAdapter` contract, one concrete RLC-423S adapter, contract tests, and a short extension doc.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-hardening-modular-expansion-base/05-CONTEXT.md
@.planning/phases/05-hardening-modular-expansion-base/05-RESEARCH.md
@.planning/phases/05-hardening-modular-expansion-base/05-VALIDATION.md
@.planning/phases/05-hardening-modular-expansion-base/05-UI-SPEC.md
@.planning/phases/01-camera-connectivity-capability-map/01-03-SUMMARY.md
@.planning/phases/02-browser-live-view-pipeline/02-02-SUMMARY.md
@.planning/phases/03-ptz-control-surface/03-01-SUMMARY.md
@.planning/phases/04-settings-safe-configuration/04-01-SUMMARY.md
@src/camera/capability-snapshot.ts
@src/camera/reolink-discovery.ts
@src/camera/reolink-live-streams.ts
@src/camera/reolink-ptz.ts
@src/camera/reolink-settings.ts

<interfaces>
From src/camera/capability-snapshot.ts:
```ts
export type CapabilitySnapshot = {
  identity: CameraIdentity;
  supportsLiveView: boolean;
  supportsPtzControl: boolean;
  supportsPtzPreset: boolean;
  supportsConfigRead: boolean;
};

export function buildCapabilitySnapshot(input: {
  identity: CameraIdentity;
  ports: ReolinkNetPort;
  ability: ReolinkAbility;
}): CapabilitySnapshot;
```

Current RLC-423S seams that must be adapter-owned by the end of this plan:
```ts
export async function probeCamera(session: ReolinkRequestExecutor): Promise<...>;
export function resolveReolinkLiveStreams(config: CameraConfig, snapshot: CapabilitySnapshot): ResolvedReolinkLiveStreams;
export function createReolinkPtzService(...): PtzService;
export function createReolinkSettingsService(...): SettingsService;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the shared adapter contract, registry, and contract-first tests</name>
  <files>src/camera/adapters/camera-adapter.ts, src/camera/adapters/index.ts, tests/camera/adapter-contract.test.ts, docs/camera-adapters.md</files>
  <read_first>src/camera/reolink-discovery.ts, src/camera/reolink-live-streams.ts, src/camera/reolink-ptz.ts, src/camera/reolink-settings.ts, src/camera/capability-snapshot.ts, .planning/phases/05-hardening-modular-expansion-base/05-CONTEXT.md, .planning/phases/05-hardening-modular-expansion-base/05-RESEARCH.md, .planning/phases/05-hardening-modular-expansion-base/05-VALIDATION.md</read_first>
  <acceptance_criteria>
    - `Test-Path src/camera/adapters/camera-adapter.ts` and `Test-Path src/camera/adapters/index.ts` both succeed.
    - `Select-String -Path src/camera/adapters/camera-adapter.ts -Pattern "export interface CameraAdapter|adapterId|matchesIdentity|probe|buildCapabilitySnapshot|resolveLiveStreams|createPtzService|createSettingsService|classifyFailure"` returns matches.
    - `Select-String -Path tests/camera/adapter-contract.test.ts -Pattern "describe\\(\"camera adapter contract\"|adapterId|matchesIdentity|resolveLiveStreams|createPtzService|createSettingsService"` returns matches.
    - `Select-String -Path docs/camera-adapters.md -Pattern "CameraAdapter|RLC-423S|future model|fixtures"` returns matches.
    - `npx vitest run tests/camera/adapter-contract.test.ts --project node` passes.
  </acceptance_criteria>
  <behavior>
    - Test 1: The contract exposes one adapter object that owns discovery, capability normalization, stream resolution, PTZ service creation, settings service creation, and diagnostics classification per D-03.
    - Test 2: The registry exposes the known RLC-423S adapter without leaking raw CGI command names or firmware suffixes into shared contract files per D-04.
    - Test 3: The documentation names the exact extension steps for a future model adapter and references fixtures as the proof source per D-10.
  </behavior>
  <action>Create `src/camera/adapters/camera-adapter.ts` with the exact top-level interface the repo will use going forward: `adapterId: string`, `matchesIdentity(identity: CameraIdentity): boolean`, `probe(session: ReolinkRequestExecutor): Promise<{ identity: CameraIdentity; devInfo: ReolinkDeviceInfo; ports: ReolinkNetPort; ability: ReolinkAbility; rawAbility: ReolinkAbility; }>` , `buildCapabilitySnapshot(input): CapabilitySnapshot`, `resolveLiveStreams(config: CameraConfig, snapshot: CapabilitySnapshot): ResolvedReolinkLiveStreams`, `createPtzService(options): PtzService`, `createSettingsService(options): SettingsService`, and `classifyFailure(scope: "probe" | "live-view" | "ptz" | "settings", error: unknown): string`. Create `src/camera/adapters/index.ts` exporting `registerCameraAdapters()`, `getRegisteredCameraAdapters()`, and `resolveCameraAdapter(identity: CameraIdentity)` with the RLC-423S adapter as the only registration for Phase 5. Write `tests/camera/adapter-contract.test.ts` first so the contract shape, registry surface, and one concrete adapter expectation are pinned before implementation. Add `docs/camera-adapters.md` describing the concrete future-model workflow: implement `CameraAdapter`, add fixture-backed tests, register the adapter in `src/camera/adapters/index.ts`, and keep model quirks inside adapter-owned modules per D-03, D-04, and D-10.</action>
  <verify>
    <automated>npx vitest run tests/camera/adapter-contract.test.ts --project node</automated>
  </verify>
  <done>The repo has one named adapter contract, one registry surface, a failing-or-green contract test file, and explicit extension documentation before any consumer rewiring happens.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the RLC-423S adapter and move existing camera seams behind it</name>
  <files>src/camera/adapters/reolink-rlc-423s-adapter.ts, src/camera/adapters/index.ts, src/camera/reolink-discovery.ts, src/camera/reolink-live-streams.ts, src/camera/reolink-ptz.ts, src/camera/reolink-settings.ts, tests/camera/adapter-contract.test.ts</files>
  <read_first>src/camera/adapters/camera-adapter.ts, src/camera/reolink-discovery.ts, src/camera/reolink-live-streams.ts, src/camera/reolink-ptz.ts, src/camera/reolink-settings.ts, tests/camera/adapter-contract.test.ts, .planning/phases/05-hardening-modular-expansion-base/05-RESEARCH.md</read_first>
  <acceptance_criteria>
    - `Test-Path src/camera/adapters/reolink-rlc-423s-adapter.ts` succeeds.
    - `Select-String -Path src/camera/adapters/reolink-rlc-423s-adapter.ts -Pattern "createRlc423sAdapter|adapterId|matchesIdentity|probe|buildCapabilitySnapshot|resolveLiveStreams|createPtzService|createSettingsService|classifyFailure"` returns matches.
    - `Select-String -Path src/camera/reolink-live-streams.ts -Pattern "export function resolveReolinkLiveStreams"` and `Select-String -Path src/camera/reolink-ptz.ts -Pattern "export function createReolinkPtzService"` and `Select-String -Path src/camera/reolink-settings.ts -Pattern "export function createReolinkSettingsService"` all still return matches, showing compatibility wrappers remain available.
    - `npx vitest run tests/camera/adapter-contract.test.ts tests/camera/reolink-ptz.test.ts tests/camera/reolink-settings.test.ts --project node` passes.
  </acceptance_criteria>
  <behavior>
    - Test 1: The concrete RLC-423S adapter returns `adapterId` `reolink-rlc-423s` and matches identities whose model contains `RLC-423S`.
    - Test 2: The adapter delegates to the existing discovery, live-stream, PTZ, and settings code without changing browser or route contracts in this plan.
    - Test 3: Failure classification lives on the adapter so later plans can log one normalized error surface instead of ad hoc message parsing.
  </behavior>
  <action>Implement `src/camera/adapters/reolink-rlc-423s-adapter.ts` as the single concrete adapter for the known firmware with `adapterId = "reolink-rlc-423s"`. Its `matchesIdentity()` must return true when `identity.model` contains `RLC-423S` case-insensitively. Its `probe()` must delegate to `probeCamera()`, its `buildCapabilitySnapshot()` must delegate to the existing `buildCapabilitySnapshot()`, its `resolveLiveStreams()` must delegate to `resolveReolinkLiveStreams()`, and its service creators must delegate to `createReolinkPtzService()` and `createReolinkSettingsService()` so plan 05-02 can consume one adapter seam without changing route contracts yet. Keep the existing `reolink-discovery.ts`, `reolink-live-streams.ts`, `reolink-ptz.ts`, and `reolink-settings.ts` exports intact as compatibility wrappers or implementation helpers, but make the new adapter the documented owner of those seams per D-03 and D-04. Implement `classifyFailure()` with the exact scope strings `probe`, `live-view`, `ptz`, and `settings`, mapping missing files to `Required files are missing`, access issues to `Access was denied`, timeout text to `Request timed out`, unsupported/model mismatch cases to `Firmware or model is unsupported`, and all other cases to `Operation failed`.</action>
  <verify>
    <automated>npx vitest run tests/camera/adapter-contract.test.ts tests/camera/reolink-ptz.test.ts tests/camera/reolink-settings.test.ts --project node</automated>
  </verify>
  <done>The current RLC-423S behavior now hangs off one adapter object with tests and docs proving where future model work belongs.</done>
</task>

</tasks>

<verification>
- [ ] `npx vitest run tests/camera/adapter-contract.test.ts --project node` succeeds
- [ ] `npx vitest run tests/camera/adapter-contract.test.ts tests/camera/reolink-ptz.test.ts tests/camera/reolink-settings.test.ts --project node` succeeds
- [ ] `docs/camera-adapters.md` names the adapter contract, registry entrypoint, and future-model extension steps explicitly
</verification>

<success_criteria>
- Phase 5 has an explicit adapter seam instead of implicit RLC-423S-only entrypoints
- Model-specific live-view, PTZ, and settings ownership is documented and testable
- Later model expansion can start from the contract instead of refactoring the v1 foundation first
</success_criteria>

<output>
After completion, create `.planning/phases/05-hardening-modular-expansion-base/05-01-SUMMARY.md`
</output>
