---
phase: 05-hardening-modular-expansion-base
plan: 03
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - web/src/App.tsx
  - web/src/components/DiagnosticsDisclosure.tsx
  - web/src/components/LiveViewerFrame.tsx
  - web/src/components/PtzPanel.tsx
  - web/src/components/SettingsPanel.tsx
  - web/src/hooks/use-live-view.ts
  - web/src/hooks/use-ptz-controls.ts
  - web/src/hooks/use-settings.ts
  - web/src/styles.css
  - tests/web/repeated-use-flows.test.tsx
  - tests/web/live-view-controls.test.tsx
autonomous: false
requirements:
  - PH5-SC2
  - PH5-SC3
  - PH5-SC4
must_haves:
  truths:
    - "The viewer-first dashboard stays visually familiar while repeated-use flows for live view, PTZ, and settings recover cleanly and keep verified state visible per D-02, D-07, and D-08."
    - "Diagnostics remain secondary in the UI, but the operator can see short actionable failure summaries plus correlation references without opening raw dumps per D-05 and D-06."
    - "Phase 5 ends with one real-camera pass that exercises connect, live view, PTZ, and settings as a continuous local loop per D-09."
  artifacts:
    - path: "tests/web/repeated-use-flows.test.tsx"
      provides: "Cross-surface jsdom coverage for retry, refresh, PTZ stop, and settings verified-state loops"
      contains: "describe(\"repeated use flows\""
    - path: "web/src/hooks/use-live-view.ts"
      provides: "Browser retry/refresh handling for live-view repeated-use loops"
      contains: "retry"
    - path: "web/src/hooks/use-ptz-controls.ts"
      provides: "PTZ status and stop-state handling for repeated pointer/release loops"
      contains: "Motion stopped"
    - path: "web/src/hooks/use-settings.ts"
      provides: "Settings verified/error/reload behavior that survives repeated interaction cycles"
      contains: "verified"
  key_links:
    - from: "web/src/hooks/use-live-view.ts"
      to: "src/server/routes/live-view.ts"
      via: "the viewer consumes backend diagnostics and retry-safe bootstrap state"
      pattern: "/api/live-view"
    - from: "web/src/hooks/use-ptz-controls.ts"
      to: "src/server/routes/ptz.ts"
      via: "PTZ commands keep hold/release/stop loops attached to the existing control plane"
      pattern: "startPtzMotion|stopPtzMotion"
    - from: "web/src/hooks/use-settings.ts"
      to: "src/server/routes/settings.ts"
      via: "settings reload and verified summaries reflect backend rereads instead of optimistic client state"
      pattern: "fetchSettingsBootstrap|applySettingsSection"
---

<objective>
Finish the user-facing hardening pass with repeated-use browser tests, low-friction viewer-first polish, and a final real-camera verification loop.

Purpose: Satisfy D-01, D-02, D-05, D-07, D-08, and D-09 by proving the existing v1 flows hold up under normal repeated use without redesigning the dashboard.
Output: Cross-surface web regression coverage, viewer-first UI polish for retry/refresh/verified loops, and one blocking manual verification checkpoint.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-hardening-modular-expansion-base/05-CONTEXT.md
@.planning/phases/05-hardening-modular-expansion-base/05-RESEARCH.md
@.planning/phases/05-hardening-modular-expansion-base/05-VALIDATION.md
@.planning/phases/05-hardening-modular-expansion-base/05-UI-SPEC.md
@.planning/phases/05-hardening-modular-expansion-base/05-02-PLAN.md
@web/src/App.tsx
@web/src/hooks/use-live-view.ts
@web/src/hooks/use-ptz-controls.ts
@web/src/hooks/use-settings.ts
@web/src/components/DiagnosticsDisclosure.tsx
@web/src/styles.css

<interfaces>
Current user-facing hook seams:
```ts
useLiveView(): {
  state: { kind: "connecting" | "live" | "reconnecting" | "failed"; reason?: string };
  retry(): Promise<void>;
}

usePtzControls(): {
  statusText: string;
  stopMotion(reason?: PtzStopReason): Promise<void>;
}

useSettings(): {
  reload(): Promise<void>;
  sections: UseSettingsSection[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock repeated-use browser flows across live view, PTZ, and settings before polishing the UI</name>
  <files>tests/web/repeated-use-flows.test.tsx, tests/web/live-view-controls.test.tsx</files>
  <read_first>tests/web/live-view-controls.test.tsx, tests/web/ptz-controls.test.tsx, tests/web/settings-panel.test.tsx, web/src/App.tsx, web/src/hooks/use-live-view.ts, web/src/hooks/use-ptz-controls.ts, web/src/hooks/use-settings.ts, .planning/phases/05-hardening-modular-expansion-base/05-VALIDATION.md, .planning/phases/05-hardening-modular-expansion-base/05-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - `Test-Path tests/web/repeated-use-flows.test.tsx` succeeds.
    - `Select-String -Path tests/web/repeated-use-flows.test.tsx -Pattern "Retry Live View|Diagnostics|Motion stopped|Verified|reload|refresh"` returns matches.
    - `Select-String -Path tests/web/live-view-controls.test.tsx -Pattern "Retry Live View|Diagnostics"` still returns matches after the new coverage is added.
    - `npx vitest run tests/web/repeated-use-flows.test.tsx tests/web/live-view-controls.test.tsx --project web` passes.
  </acceptance_criteria>
  <behavior>
    - Test 1: Live-view retry and reconnect loops keep the exact `Retry Live View` action and preserve diagnostics as secondary disclosure content per D-05 and D-08.
    - Test 2: PTZ repeated hold/release/explicit-stop flows leave the UI in `Motion stopped` or equivalent ready state instead of an ambiguous busy state per D-07.
    - Test 3: Settings verify/reload loops preserve verified summaries and recover from refresh/reload without losing the supported section structure per D-02 and D-09.
  </behavior>
  <action>Create `tests/web/repeated-use-flows.test.tsx` to cover one continuous repeated-use matrix: live-view failure followed by `Retry Live View`, PTZ hold/release followed by explicit stop visibility, and settings verified/reload cycles after a successful apply. Reuse the existing mocking style from `tests/web/live-view-controls.test.tsx`, `tests/web/ptz-controls.test.tsx`, and `tests/web/settings-panel.test.tsx` so the new file stays fast and deterministic. Keep the exact visible copy strings from the existing UI contract: `Retry Live View`, `Diagnostics`, `Motion stopped`, and `Verified`. Extend `tests/web/live-view-controls.test.tsx` only where necessary to preserve the collapsed-diagnostics expectation while the repeated-use suite focuses on cross-surface loops.</action>
  <verify>
    <automated>npx vitest run tests/web/repeated-use-flows.test.tsx tests/web/live-view-controls.test.tsx --project web</automated>
  </verify>
  <done>The phase now has a dedicated browser regression suite that defines repeated-use expectations before runtime/UI polish is applied.</done>
</task>

<task type="auto">
  <name>Task 2: Polish the viewer-first dashboard for retry, refresh, verified state, and secondary diagnostics</name>
  <files>web/src/App.tsx, web/src/components/DiagnosticsDisclosure.tsx, web/src/components/LiveViewerFrame.tsx, web/src/components/PtzPanel.tsx, web/src/components/SettingsPanel.tsx, web/src/hooks/use-live-view.ts, web/src/hooks/use-ptz-controls.ts, web/src/hooks/use-settings.ts, web/src/styles.css, tests/web/repeated-use-flows.test.tsx, tests/web/live-view-controls.test.tsx</files>
  <read_first>web/src/App.tsx, web/src/components/DiagnosticsDisclosure.tsx, web/src/components/LiveViewerFrame.tsx, web/src/components/PtzPanel.tsx, web/src/components/SettingsPanel.tsx, web/src/hooks/use-live-view.ts, web/src/hooks/use-ptz-controls.ts, web/src/hooks/use-settings.ts, tests/web/repeated-use-flows.test.tsx, .planning/phases/05-hardening-modular-expansion-base/05-CONTEXT.md, .planning/phases/05-hardening-modular-expansion-base/05-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - `Select-String -Path web/src/App.tsx -Pattern "DiagnosticsDisclosure|SettingsPanel|PtzPanel|LiveViewerFrame"` returns matches proving the viewer-first shell remains intact.
    - `Select-String -Path web/src/hooks/use-live-view.ts -Pattern "retry|reconnecting|reason|requestId|artifactPath"` returns matches showing retry and secondary diagnostics handling.
    - `Select-String -Path web/src/hooks/use-settings.ts -Pattern "reload|verified|sectionError"` returns matches.
    - `Select-String -Path web/src/components/DiagnosticsDisclosure.tsx -Pattern "requestId|artifactPath|Diagnostics"` returns matches.
    - `Select-String -Path web/src/styles.css -Pattern "diagnostics|verified|status|44px"` returns matches.
    - `npm run build:web` passes.
    - `npx vitest run tests/web/repeated-use-flows.test.tsx tests/web/live-view-controls.test.tsx --project web` passes.
  </acceptance_criteria>
  <action>Update the existing viewer-first shell without reordering the major layout blocks in `App.tsx` per D-08. In `use-live-view.ts`, preserve the current retry and fallback semantics but keep a short operator-facing reason plus optional `requestId` and `artifactPath` reference from the backend so the main frame can say what failed without dumping protocol detail per D-05 and D-06. In `use-ptz-controls.ts`, make repeated hold/release and explicit stop loops converge on a stable ready message after stop completion, not a lingering transient failure state, and keep the explicit stop button behavior intact. In `use-settings.ts`, make `reload()` restore the latest bootstrap and verified state cleanly after repeated edits, and ensure section-local errors clear when a new edit begins. In `DiagnosticsDisclosure.tsx` and `LiveViewerFrame.tsx`, keep diagnostics secondary and collapsed by default, but show short correlation references such as `requestId` and `artifactPath` labels when present. Adjust `styles.css` only enough to strengthen repeated-use trust cues already in the design contract: visible verified state, stable status spacing, minimum 44px controls, and no late-phase dashboard redesign per D-07 and D-08.</action>
  <verify>
    <automated>npm run build:web && npx vitest run tests/web/repeated-use-flows.test.tsx tests/web/live-view-controls.test.tsx --project web</automated>
  </verify>
  <done>The existing dashboard shell now handles repeated-use loops more cleanly while keeping diagnostics secondary and preserving the approved visual structure.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run one real-camera repeated-use hardening pass</name>
  <files>src/index.ts, web/src/App.tsx, web/src/hooks/use-live-view.ts, web/src/hooks/use-ptz-controls.ts, web/src/hooks/use-settings.ts</files>
  <read_first>.planning/phases/05-hardening-modular-expansion-base/05-VALIDATION.md, src/index.ts, web/src/App.tsx, web/src/hooks/use-live-view.ts, web/src/hooks/use-ptz-controls.ts, web/src/hooks/use-settings.ts</read_first>
  <acceptance_criteria>
    - `npm test` passes before the manual check starts.
    - The operator can complete all numbered verification steps below on the real RLC-423S without the dashboard requiring a process restart.
    - The operator reports either `approved` or a concrete issue list referencing the failing step numbers.
  </acceptance_criteria>
  <action>Run the real-camera hardening pass after Tasks 1 and 2 are green. Use the existing local app entrypoint and dashboard, keep the viewer-first layout intact, and verify the exact repeated-use loop from D-01, D-02, D-05, D-07, D-08, and D-09: reconnect live view without restarting the app, stop PTZ reliably after motion, apply one safe setting and confirm the verified reread state, then confirm diagnostics stay secondary while surfacing short correlation references.</action>
  <what-built>Adapter-aware backend diagnostics, repeated-use browser handling, and viewer-first polish across connect, live view, PTZ, and settings.</what-built>
  <how-to-verify>
    1. Start the app with the real camera config and open the dashboard.
    2. Confirm the viewer connects, then trigger `Retry Live View` at least once by interrupting or reselecting a mode and verify playback recovers without restarting the app.
    3. Hold a PTZ direction, release it, then press the explicit stop control once; confirm the UI lands on a clear stopped/ready state and the camera does not keep moving.
    4. Apply one safe settings change, confirm a verified result appears, then refresh/reload settings and confirm the section still reflects the camera-reported value.
    5. Open diagnostics only after one failure or retry event and confirm the disclosure shows short reason text plus correlation references, not raw CGI payloads or credentials.
  </how-to-verify>
  <resume-signal>Type "approved" if all five steps pass, or reply with the failing step numbers and observed behavior.</resume-signal>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>A human has exercised the core local loop on the real camera and either approved the phase or provided concrete defects for follow-up.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build:web` succeeds
- [ ] `npx vitest run tests/web/repeated-use-flows.test.tsx tests/web/live-view-controls.test.tsx --project web` succeeds
- [ ] `npm test` succeeds before the blocking manual verification task
- [ ] One real-camera repeated-use pass is completed and acknowledged
</verification>

<success_criteria>
- Core user flows hold up under repeated local use without redesigning the dashboard
- Diagnostics remain secondary but actionable in the browser
- Phase 5 finishes with a concrete operator validation loop, not just fixture confidence
</success_criteria>

<output>
After completion, create `.planning/phases/05-hardening-modular-expansion-base/05-03-SUMMARY.md`
</output>
