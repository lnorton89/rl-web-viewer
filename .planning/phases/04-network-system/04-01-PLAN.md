---
wave: 1
depends_on: []
files_modified:
  - src/types/settings.ts
  - src/camera/reolink-settings.ts
  - src/server/routes/settings.ts
  - web/src/components/SettingsPanel.tsx
  - web/src/lib/settings-api.ts
autonomous: true
requirements_addressed:
  - NET-01
  - NET-02
---

<objective>

Add network information display and editing to the Settings panel. Show IP address, subnet, gateway, MAC, DNS. Allow editing with validation and confirmation.

</objective>

<tasks>

<task name="Add network section to settings types">
<read_first>
  src/types/settings.ts
</read_first>
<action>

1. Add "network" to SETTINGS_SECTION_IDS
2. Add "network" to EDITABLE_SETTINGS_SECTION_IDS (since we're allowing edits)
3. Add network field specs with kind "text" for IP addresses

Example fields:
- `network.ip` — IP Address
- `network.subnet` — Subnet Mask  
- `network.gateway` — Gateway
- `network.mac` — MAC Address (read only)
- `network.dns` — DNS Server

</action>
<acceptance_criteria>

- "network" appears in SETTINGS_SECTION_IDS
- "network" appears in EDITABLE_SETTINGS_SECTION_IDS
- Network field specs defined with proper constraints

</acceptance_criteria>
</task>

<task name="Add network fields to camera settings service">
<read_first>
  src/camera/reolink-settings.ts
</read_first>
<action>

1. Add network fields to the settings bootstrap
2. Return mock/placeholder values for now:
   - IP: "192.168.1.100"
   - Subnet: "255.255.255.0"
   - Gateway: "192.168.1.1"
   - MAC: "AA:BB:CC:DD:EE:FF"
   - DNS: "8.8.8.8"

3. Add apply handler for network section that validates and accepts changes

</action>
<acceptance_criteria>

- Network section appears in settings bootstrap
- Field specs match the types added in task 1
- Apply returns success response

</acceptance_criteria>
</task>

<task name="Update SettingsPanel to show network info">
<read_first>
  web/src/components/SettingsPanel.tsx
</read_first>
<action>

1. Add NetworkSettingsSection component or extend existing to show network info
2. Display current values from settings API
3. Show IP, subnet, gateway, MAC, DNS in a read-friendly format
4. Add edit mode for editable fields (IP, subnet, gateway, DNS)
5. Include validation feedback

</action>
<acceptance_criteria>

- Network info visible in Settings panel
- Current values displayed
- Editable fields have input controls
- Validation feedback shown

</acceptance_criteria>
</task>

<task name="Add confirmation dialog for network changes">
<read_first>
  web/src/components/SettingsPanel.tsx
</read_first>
<action>

1. Before applying network changes, show confirmation dialog
2. Warning text: "Changing network settings may disconnect the camera"
3. Require user acknowledgment before sending apply request
4. Show success/failure feedback after apply

</action>
<acceptance_criteria>

- Confirmation dialog appears before apply
- Warning message explains risk
- User must confirm before changes are sent
- Success/error feedback displayed after apply

</acceptance_criteria>
</task>

<task name="Add tests for network settings">
<action>

1. Add test for network section rendering
2. Add test for validation
3. Add test for confirmation dialog

</action>
<acceptance_criteria>

- Tests cover network info display
- Tests cover form validation
- Tests cover confirmation flow

</acceptance_criteria>
</task>

</tasks>

<verification>

## Verification Checklist

- [ ] `npm run build` completes without errors
- [ ] `npm test` passes
- [ ] Network section visible in Settings
- [ ] Current values displayed
- [ ] Edit form works
- [ ] Validation feedback shown
- [ ] Confirmation dialog appears before apply

</verification>

<must_haves>

## Must-Have Criteria (Goal Validation)

1. **Network info visible** — IP, subnet, gateway, MAC, DNS shown in Settings
2. **Editable fields functional** — User can modify IP, subnet, gateway, DNS
3. **Validation works** — Invalid input shows feedback
4. **Confirmation required** — User must confirm before network changes apply
5. **Graceful handling** — Works when camera doesn't return network info

</must_haves>
