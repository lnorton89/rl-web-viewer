---
phase: 01-ui-modernization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/main.tsx
  - web/src/App.tsx
  - web/src/styles.css
  - web/src/theme/theme.ts
  - web/src/theme/DarkTheme.ts
  - web/src/components/LayoutShell.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
---

<objective>
Set up MUI foundation — install dependencies, create dark theme matching current palette, and build the main layout shell with sidebar navigation.
</objective>

<context>
@.planning/phases/01-ui-modernization/01-CONTEXT.md
@web/src/App.tsx
@web/src/styles.css
@web/src/main.tsx
</context>

<interfaces>
From web/src/App.tsx (current component structure):
```tsx
export default function App() {
  const { /* useLiveView state */ } = useLiveView();
  return (
    <div className="app-shell">
      <main className="viewer-dashboard">
        <header className="viewer-hero">...</header>
        <div className="viewer-ptz-cluster">
          <LiveViewerFrame />
          <PtzPanel />
        </div>
        <ModeSwitcher />
        <SettingsPanel />
        <DiagnosticsDisclosure />
      </main>
    </div>
  );
}
```

Current color palette from styles.css (preserve in MUI theme):
- `--color-dominant`: #12161b
- `--color-secondary`: #1d242c
- `--color-accent`: #63c7b2
- `--color-border`: rgba(255, 255, 255, 0.08)
- `--color-muted`: #97a4ad
- `--color-danger`: #c65a5a
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install MUI dependencies</name>
  <files>package.json</files>
  <read_first>
    - package.json (current dependencies)
  </read_first>
  <action>
    Add MUI dependencies to package.json devDependencies section:
    ```json
    "@mui/material": "^7.0.0",
    "@mui/icons-material": "^7.0.0",
    "@emotion/react": "^11.13.0",
    "@emotion/styled": "^11.13.0"
    ```
    Then run: npm install
  </action>
  <acceptance_criteria>
    - grep "@mui/material" package.json returns the dependency
    - grep "@emotion/react" package.json returns the dependency
    - npm install completes without errors
  </acceptance_criteria>
  <verify>
    <automated>grep -q "@mui/material" package.json && grep -q "@emotion/react" package.json && echo "PASS"</automated>
  </verify>
  <done>MUI dependencies installed, no package.json conflicts</done>
</task>

<task type="auto">
  <name>Task 2: Create MUI dark theme</name>
  <files>
    - web/src/theme/theme.ts
    - web/src/theme/DarkTheme.tsx
    - web/src/theme/index.ts
  </files>
  <read_first>
    - web/src/styles.css (extract current color palette)
  </read_first>
  <action>
    Create web/src/theme/ directory with MUI theme configuration:

    1. web/src/theme/theme.ts — Create MUI theme object:
    ```typescript
    import { createTheme } from '@mui/material/styles';

    export const darkTheme = createTheme({
      palette: {
        mode: 'dark',
        primary: {
          main: '#63c7b2', // accent color
        },
        secondary: {
          main: '#1d242c',
        },
        background: {
          default: '#12161b',
          paper: '#1d242c',
        },
        text: {
          primary: '#ffffff',
          secondary: '#97a4ad',
        },
        error: {
          main: '#c65a5a',
        },
        divider: 'rgba(255, 255, 255, 0.08)',
      },
      typography: {
        fontFamily: 'system-ui, -apple-system, sans-serif',
      },
      components: {
        MuiButton: {
          styleOverrides: {
            root: { textTransform: 'none' },
          },
        },
      },
    });
    ```

    2. web/src/theme/DarkTheme.tsx — Theme provider component:
    ```tsx
    import { ThemeProvider } from '@mui/material/styles';
    import CssBaseline from '@mui/material/CssBaseline';
    import { ReactNode } from 'react';
    import { darkTheme } from './theme';

    interface DarkThemeProps {
      children: ReactNode;
    }

    export function DarkTheme({ children }: DarkThemeProps) {
      return (
        <ThemeProvider theme={darkTheme}>
          <CssBaseline />
          {children}
        </ThemeProvider>
      );
    }
    ```

    3. web/src/theme/index.ts — Export the theme:
    ```typescript
    export { darkTheme } from './theme';
    export { DarkTheme } from './DarkTheme';
    ```
  </action>
  <acceptance_criteria>
    - web/src/theme/theme.ts exports darkTheme with correct color values
    - web/src/theme/DarkTheme.tsx wraps children with ThemeProvider and CssBaseline
    - Theme colors match CSS variables: primary main is #63c7b2, background default is #12161b
  </acceptance_criteria>
  <verify>
    <automated>grep -q "primary:.*main.*#63c7b2" web/src/theme/theme.ts && grep -q "background:.*default.*#12161b" web/src/theme/theme.ts && echo "PASS"</automated>
  </verify>
  <done>Dark theme created with exact color palette from current CSS</done>
</task>

<task type="auto">
  <name>Task 3: Create LayoutShell component</name>
  <files>web/src/components/LayoutShell.tsx</files>
  <read_first>
    - web/src/theme/DarkTheme.tsx
    - web/src/App.tsx
  </read_first>
  <action>
    Create web/src/components/LayoutShell.tsx implementing the sidebar navigation layout:

    ```tsx
    import { ReactNode } from 'react';
    import { Box, Drawer, List, ListItemButton, ListItemIcon, ListItemText, AppBar, Toolbar, Typography } from '@mui/material';
    import { Videocam, Settings, Joystick } from '@mui/icons-material';

    interface NavItem {
      id: string;
      label: string;
      icon: ReactNode;
    }

    const NAV_ITEMS: NavItem[] = [
      { id: 'live', label: 'Live View', icon: <Videocam /> },
      { id: 'ptz', label: 'PTZ', icon: <Joystick /> },
      { id: 'settings', label: 'Settings', icon: <Settings /> },
    ];

    const SIDEBAR_WIDTH = 200;

    interface LayoutShellProps {
      children: ReactNode;
      activeSection: string;
      onSectionChange: (section: string) => void;
    }

    export function LayoutShell({ children, activeSection, onSectionChange }: LayoutShellProps) {
      return (
        <Box sx={{ display: 'flex', minHeight: '100vh' }}>
          {/* Sidebar */}
          <Drawer
            variant="permanent"
            sx={{
              width: SIDEBAR_WIDTH,
              flexShrink: 0,
              '& .MuiDrawer-paper': {
                width: SIDEBAR_WIDTH,
                boxSizing: 'border-box',
                borderRight: '1px solid rgba(255, 255, 255, 0.08)',
              },
            }}
          >
            <Toolbar>
              <Typography variant="h6" noWrap>
                Reolink
              </Typography>
            </Toolbar>
            <List>
              {NAV_ITEMS.map((item) => (
                <ListItemButton
                  key={item.id}
                  selected={activeSection === item.id}
                  onClick={() => onSectionChange(item.id)}
                >
                  <ListItemIcon>{item.icon}</ListItemIcon>
                  <ListItemText primary={item.label} />
                </ListItemButton>
              ))}
            </List>
          </Drawer>

          {/* Main content */}
          <Box
            component="main"
            sx={{
              flexGrow: 1,
              p: 3,
              overflow: 'auto',
              height: '100vh',
            }}
          >
            {children}
          </Box>
        </Box>
      );
    }
    ```

    The layout:
    - Sidebar fixed at 200px width
    - Contains navigation for Live View, PTZ, Settings
    - Main content area takes remaining space
    - Fits 1920x1080 without scrolling per UI-01
  </action>
  <acceptance_criteria>
    - LayoutShell.tsx exports LayoutShell component with sidebar + main content
    - Sidebar width is 200px (SIDEBAR_WIDTH constant)
    - Navigation items exist for 'live', 'ptz', 'settings'
    - Component accepts activeSection and onSectionChange props
    - Main content uses flexGrow: 1 to fill remaining space
  </acceptance_criteria>
  <verify>
    <automated>grep -q "SIDEBAR_WIDTH = 200" web/src/components/LayoutShell.tsx && grep -q "activeSection.*onSectionChange" web/src/components/LayoutShell.tsx && echo "PASS"</automated>
  </verify>
  <done>LayoutShell component created with fixed 200px sidebar and responsive main content</done>
</task>

<task type="auto">
  <name>Task 4: Update main.tsx with ThemeProvider</name>
  <files>web/src/main.tsx</files>
  <read_first>
    - web/src/main.tsx
    - web/src/theme/DarkTheme.tsx
  </read_first>
  <action>
    Update web/src/main.tsx to wrap the App with DarkTheme provider:

    Replace the import section and render:
    ```tsx
    import { StrictMode } from 'react';
    import { createRoot } from 'react-dom/client';
    import { DarkTheme } from './theme/DarkTheme.js';
    import App from './App.js';

    const root = document.getElementById('root')!;
    createRoot(root).render(
      <StrictMode>
        <DarkTheme>
          <App />
        </DarkTheme>
      </StrictMode>
    );
    ```
  </action>
  <acceptance_criteria>
    - main.tsx imports DarkTheme from ./theme/DarkTheme.js
    - App is wrapped with DarkTheme component
    - CssBaseline from MUI is applied globally
  </acceptance_criteria>
  <verify>
    <automated>grep -q "DarkTheme" web/src/main.tsx && echo "PASS"</automated>
  </verify>
  <done>Dark theme applied globally via ThemeProvider in main.tsx</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→server | Untrusted user input in navigation state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Navigation state | accept | Section IDs are internal, not user-provided strings |
| T-01-02 | Information Disclosure | Theme colors | accept | Colors are public display information |
</threat_model>

<verification>
- MUI dependencies installed without conflicts
- Theme colors match current CSS palette exactly
- LayoutShell renders with 200px fixed sidebar
- Main content fills remaining viewport
</verification>

<success_criteria>
1. MUI installed and no build errors
2. Dark theme created with exact color palette (#63c7b2 accent, #12161b background)
3. LayoutShell renders sidebar + main content
4. App wrapped with DarkTheme provider
5. No TypeScript errors from new imports
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-modernization/01-01-SUMMARY.md`
</output>
