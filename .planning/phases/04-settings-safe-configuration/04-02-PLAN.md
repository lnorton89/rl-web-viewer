---
phase: 04-settings-safe-configuration
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/server/routes/settings.ts
  - src/server/create-server.ts
  - tests/server/settings-routes.test.ts
autonomous: true
requirements:
  - CONF-01
  - CONF-03
  - CONF-04
must_haves:
  truths:
    - "The browser can load the settings bootstrap through the Fastify control plane without seeing camera credentials, tokens, or raw CGI payloads."
    - "Each section apply request is validated and returns precise field-level or section-level feedback instead of a generic save failure."
    - "Successful apply responses already reflect reread-verified camera state, so the browser does not have to trust optimistic local state."
  artifacts:
    - path: "src/server/routes/settings.ts"
      provides: "Browser-safe settings bootstrap and section-apply endpoints"
      contains: "/api/settings"
    - path: "src/server/create-server.ts"
      provides: "Mounted Fastify registration for the settings route plugin"
      contains: "settingsRoutes"
    - path: "tests/server/settings-routes.test.ts"
      provides: "Route coverage for validation, capability gating, browser-safe responses, and verified apply results"
      contains: "describe(\"settings routes\""
  key_links:
    - from: "src/server/routes/settings.ts"
      to: "src/camera/reolink-settings.ts"
      via: "settings bootstrap and apply requests delegate to the backend-owned settings service"
      pattern: "createReolinkSettingsService|SettingsService"
    - from: "src/server/create-server.ts"
      to: "src/server/routes/settings.ts"
      via: "the app server mounts the settings plugin beside live view and PTZ"
      pattern: "register\\(settingsRoutes"
    - from: "src/server/routes/settings.ts"
      to: "src/types/settings.ts"
      via: "all request and response payloads use shared settings DTOs"
      pattern: "SettingsBootstrap|SettingsApplySuccess|SettingsApplyFailure"
---

<objective>
Expose the Phase 4 settings service through browser-safe Fastify routes and mount the settings plugin on the existing local control plane.

Purpose: Keep all settings reads and writes behind the Node app so the browser never sees camera credentials and every apply path stays validated, capability-aware, and verified.
Output: `settingsRoutes`, updated server registration, and route tests for bootstrap, validation, apply failures, and verified responses.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-settings-safe-configuration/04-CONTEXT.md
@.planning/phases/04-settings-safe-configuration/04-RESEARCH.md
@.planning/phases/04-settings-safe-configuration/04-VALIDATION.md
@.planning/phases/03-ptz-control-surface/03-02-SUMMARY.md
@src/server/create-server.ts
@src/server/routes/live-view.ts
@src/server/routes/ptz.ts

<interfaces>
From plan 04-01 (`src/types/settings.ts`):
```ts
export type SettingsSectionId = "time" | "osd" | "image" | "stream" | "isp";
export type EditableSettingsSectionId = "time" | "osd" | "image" | "stream";
export type SettingsBootstrap = {
  sections: SettingsSection[];
};

export interface SettingsService {
  getBootstrap(): Promise<SettingsBootstrap>;
  applySection(
    sectionId: EditableSettingsSectionId,
    draft: unknown,
  ): Promise<SettingsApplySuccess | SettingsApplyFailure>;
}
```

Existing Fastify plugin pattern from src/server/routes/ptz.ts:
```ts
export type PtzRouteDependencies = {
  createPtzService?: () => Promise<PtzService> | PtzService;
};

export const ptzRoutes: FastifyPluginAsync<PtzRouteDependencies> = async (
  app,
  options,
) => { /* register browser-safe routes */ };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the settings route plugin with exact section apply contracts and precise error payloads</name>
  <files>src/server/routes/settings.ts, tests/server/settings-routes.test.ts</files>
  <read_first>src/server/routes/live-view.ts, src/server/routes/ptz.ts, src/types/settings.ts, src/camera/reolink-settings.ts, tests/server/ptz-routes.test.ts, .planning/phases/04-settings-safe-configuration/04-RESEARCH.md, .planning/phases/04-settings-safe-configuration/04-VALIDATION.md</read_first>
  <acceptance_criteria>
    - `Test-Path src/server/routes/settings.ts` succeeds and `Select-String -Path src/server/routes/settings.ts -Pattern "/api/settings|/apply|time|osd|image|stream"` returns matches for the required endpoints and section ids.
    - `Select-String -Path src/server/routes/settings.ts -Pattern "400|409|422|fieldErrors|sectionError"` returns matches showing malformed requests, unsupported sections, and precise failure shapes are handled separately.
    - `Select-String -Path tests/server/settings-routes.test.ts -Pattern "browser-safe|field errors|section-level|verified"` returns matches for bootstrap safety and apply-result coverage.
    - `npx vitest run tests/server/settings-routes.test.ts --project node -x` passes.
  </acceptance_criteria>
  <behavior>
    - Test 1: `GET /api/settings` returns a browser-safe `SettingsBootstrap` with editable and read-only sections but no camera credentials.
    - Test 2: `POST /api/settings/:sectionId/apply` validates the section-specific draft and returns `422` with `fieldErrors` or `sectionError` when the change is invalid or rejected.
    - Test 3: Successful apply responses return reread-verified before/after summaries instead of optimistic echoes.
  </behavior>
  <action>Create `src/server/routes/settings.ts` as a Fastify plugin that mirrors the existing live-view and PTZ route shape. Export `SettingsRouteDependencies = { createSettingsService?: () => Promise<SettingsService> | SettingsService }` for inject-based tests. Implement `GET /api/settings` to return the `SettingsBootstrap` from plan 04-01 and reject unsafe payloads if serialized output contains credentials, tokens, raw `cgi-bin/api.cgi` URLs, or `rtsp://` values. Implement `POST /api/settings/:sectionId/apply` for the exact editable ids `time`, `osd`, `image`, and `stream`; validate the route param and the section-specific draft with `zod`; return `400` for malformed params or body shape, `409` for unsupported/uneditable sections per D-10, `422` with `{ fieldErrors, sectionError }` for route-validation or camera-reject failures per D-07 and D-08, and `200` with the verified apply result from the service for successful writes per D-05. The route layer must keep `isp` and all deferred categories read-only by refusing any apply attempt outside the approved subset from D-01 and D-02.</action>
  <verify>
    <automated>npx vitest run tests/server/settings-routes.test.ts --project node -x</automated>
  </verify>
  <done>The repo has a browser-safe settings route plugin with exact section ids, route validation, and precise failure payloads for field-level and section-level feedback.</done>
</task>

<task type="auto">
  <name>Task 2: Mount settings routes in the app server and finish control-plane coverage</name>
  <files>src/server/create-server.ts, src/server/routes/settings.ts, tests/server/settings-routes.test.ts</files>
  <read_first>src/server/create-server.ts, src/server/routes/settings.ts, src/server/routes/live-view.ts, src/server/routes/ptz.ts, tests/server/settings-routes.test.ts, .planning/phases/03-ptz-control-surface/03-02-SUMMARY.md</read_first>
  <acceptance_criteria>
    - `Select-String -Path src/server/create-server.ts -Pattern "settingsRoutes|settings\\?:"` returns matches showing the server factory accepts settings dependencies and registers the plugin.
    - `Select-String -Path tests/server/settings-routes.test.ts -Pattern "createServer|/api/settings|/api/live-view|/api/ptz"` returns matches proving the mounted control plane is exercised alongside existing routes.
    - `npx vitest run tests/server/settings-routes.test.ts --project node -x` passes.
    - `npm run build:server` passes.
  </acceptance_criteria>
  <action>Extend `createServer()` to accept `settings?: SettingsRouteDependencies` and register `settingsRoutes` after `ptzRoutes` so the local app exposes live view, PTZ, and settings from one Fastify surface. Finish `tests/server/settings-routes.test.ts` by creating `createServer({ staticRoot, liveView: ..., ptz: ..., settings: { createSettingsService: ... } })` cases that assert the mounted app serves `/api/settings`, preserves existing `/api/live-view` and `/api/ptz` behavior, and returns the exact status codes and response shapes from Task 1. Keep the browser boundary intact: the mounted server test should fail if a settings response leaks credentials or raw camera URLs.</action>
  <verify>
    <automated>npm run build:server && npx vitest run tests/server/settings-routes.test.ts --project node -x</automated>
  </verify>
  <done>The Fastify app now exposes a complete browser-safe settings API beside live view and PTZ, with integration coverage proving the control plane stays unified and safe.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build:server` succeeds
- [ ] `npx vitest run tests/server/settings-routes.test.ts --project node -x` succeeds
- [ ] `/api/settings` and `/api/settings/:sectionId/apply` are mounted through `createServer()` without breaking `/api/live-view` or `/api/ptz`
</verification>

<success_criteria>
- The browser has one Fastify-owned settings API surface to call
- Route validation and response shaping preserve precise field and section errors
- Verified apply responses stay behind the Node control plane and never expose raw camera details
</success_criteria>

<output>
After completion, create `.planning/phases/04-settings-safe-configuration/04-02-SUMMARY.md`
</output>
