---
phase: 04-settings-safe-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/settings.ts
  - src/camera/reolink-settings.ts
  - tests/camera/reolink-settings.test.ts
  - tests/fixtures/reolink/get-time.json
  - tests/fixtures/reolink/get-ntp.json
  - tests/fixtures/reolink/get-image.json
  - tests/fixtures/reolink/get-osd.json
  - tests/fixtures/reolink/get-isp.json
  - tests/fixtures/reolink/get-enc.json
autonomous: true
requirements:
  - CONF-02
  - CONF-03
  - CONF-04
must_haves:
  truths:
    - "The backend exposes one normalized settings contract that distinguishes editable sections from inspect-only sections before the browser renders anything."
    - "Every successful settings write is verified by a fresh camera re-read instead of trusting the submitted draft."
    - "Patch-friendly sections and full-object sections follow different write paths, so SetImage and SetEnc do not fail from partial payloads on the target firmware."
  artifacts:
    - path: "src/types/settings.ts"
      provides: "Shared settings DTOs, section ids, editable payloads, and verified result contracts"
      contains: "export type SettingsSectionId"
    - path: "src/camera/reolink-settings.ts"
      provides: "Capability-aware Reolink settings service with read/bootstrap/apply/verify logic"
      contains: "createReolinkSettingsService"
    - path: "tests/camera/reolink-settings.test.ts"
      provides: "Fixture-backed coverage for bootstrap normalization, setter strategy branching, error mapping, and verified rereads"
      contains: "describe(\"reolink settings service\""
  key_links:
    - from: "src/camera/reolink-settings.ts"
      to: "src/camera/reolink-session.ts"
      via: "all Get*/Set* commands flow through the shared authenticated request path"
      pattern: "requestJson"
    - from: "src/camera/reolink-settings.ts"
      to: "src/camera/capability-snapshot.ts"
      via: "supportsConfigRead gates the settings surface before editable sections are exposed"
      pattern: "loadCapabilitySnapshot|supportsConfigRead"
    - from: "src/camera/reolink-settings.ts"
      to: "src/diagnostics/debug-capture.ts"
      via: "unexpected malformed settings payloads and rejected writes emit sanitized artifacts"
      pattern: "writeDebugArtifact"
---

<objective>
Define the shared settings contracts, firmware-backed fixtures, and the backend settings service that owns read/validate/write/verify behavior for Phase 4.

Purpose: Lock the safe editable subset and the mixed setter strategy into one capability-aware backend contract before Fastify routes or React forms depend on it.
Output: `src/types/settings.ts`, a tested `src/camera/reolink-settings.ts` service, and sanitized settings fixtures for the known RLC-423S firmware.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-settings-safe-configuration/04-CONTEXT.md
@.planning/phases/04-settings-safe-configuration/04-RESEARCH.md
@.planning/phases/04-settings-safe-configuration/04-UI-SPEC.md
@.planning/phases/04-settings-safe-configuration/04-VALIDATION.md
@.planning/phases/01-camera-connectivity-capability-map/01-03-SUMMARY.md
@src/config/camera-config.ts
@src/camera/reolink-session.ts
@src/camera/capability-snapshot.ts
@src/diagnostics/debug-capture.ts

<interfaces>
From src/camera/reolink-session.ts:
```ts
export class ReolinkSession {
  async requestJson<TResponse extends readonly ReolinkApiResponse[]>(
    commands: readonly ReolinkRequest[],
  ): Promise<TResponse>;
}
```

From src/camera/capability-snapshot.ts:
```ts
export type CapabilitySnapshot = {
  supportsConfigRead: boolean;
};

export async function loadCapabilitySnapshot(
  config: CameraConfig,
  snapshotPath?: string,
): Promise<CapabilitySnapshot>;
```

Existing server-side config source from src/config/camera-config.ts:
```ts
export async function loadCameraConfig(
  configPath?: string,
): Promise<CameraConfig>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the shared settings contracts, section ids, and firmware fixtures</name>
  <files>src/types/settings.ts, tests/camera/reolink-settings.test.ts, tests/fixtures/reolink/get-time.json, tests/fixtures/reolink/get-ntp.json, tests/fixtures/reolink/get-image.json, tests/fixtures/reolink/get-osd.json, tests/fixtures/reolink/get-isp.json, tests/fixtures/reolink/get-enc.json</files>
  <read_first>src/camera/reolink-session.ts, src/camera/capability-snapshot.ts, src/types/live-view.ts, src/types/ptz.ts, tests/camera/reolink-ptz.test.ts, .planning/phases/04-settings-safe-configuration/04-CONTEXT.md, .planning/phases/04-settings-safe-configuration/04-RESEARCH.md, .planning/phases/04-settings-safe-configuration/04-VALIDATION.md</read_first>
  <acceptance_criteria>
    - `Test-Path src/types/settings.ts` succeeds and `Select-String -Path src/types/settings.ts -Pattern "export type SettingsSectionId|export interface SettingsService|export type SettingsBootstrap"` returns matches.
    - `Test-Path tests/fixtures/reolink/get-time.json`, `get-ntp.json`, `get-image.json`, `get-osd.json`, `get-isp.json`, and `get-enc.json` all succeed.
    - `Select-String -Path tests/camera/reolink-settings.test.ts -Pattern "bootstrap|supportsConfigRead|SetImage|SetEnc|verified"` returns matches showing bootstrap and mixed-strategy cases are specified first.
    - `npx vitest run tests/camera/reolink-settings.test.ts --testNamePattern "bootstrap|strategy"` passes.
  </acceptance_criteria>
  <behavior>
    - Test 1: `getBootstrap()` returns editable sections `time`, `osd`, `image`, and `stream`, plus read-only `isp`, when capability flags permit config reads.
    - Test 2: `getBootstrap()` hides or empties the settings surface when `supportsConfigRead` is false.
    - Test 3: `SetImage` and `SetEnc` flows require full-object merge inputs while `SetOsd`, `SetNtp`, and `SetTime` accept narrowed payloads.
  </behavior>
  <action>Create `src/types/settings.ts` with the exact Phase 4 shared contract surface per D-01, D-02, D-09, and D-10: `SettingsSectionId = "time" | "osd" | "image" | "stream" | "isp"`, `EditableSettingsSectionId = "time" | "osd" | "image" | "stream"`, `ReadOnlySettingsSectionId = "isp"`, `SettingsBootstrap`, `SettingsSection`, `SettingsSectionStatus`, `SettingsDiffRow`, `SettingsFieldError`, `SettingsApplySuccess`, `SettingsApplyFailure`, and `SettingsService`. Model the editable field set exactly as researched: `time` owns `Ntp.enable`, `Ntp.server`, `Ntp.port`, `Ntp.interval`, `Time.timeZone`, `Time.timeFmt`, `Time.hourFmt`, and `Dst.*` while leaving wall-clock values inspect-only; `osd` owns `osdChannel.enable`, `osdChannel.name`, `osdChannel.pos`, `osdTime.enable`, and `osdTime.pos`; `image` owns `bright`, `contrast`, `hue`, `saturation`, and `sharpen`; `stream` exposes only `subStream.size`, `subStream.frameRate`, `subStream.bitRate`, and `subStream.profile` as editable while keeping main-stream summary read-only; `isp` is inspect-only. Add sanitized `GetTime`, `GetNtp`, `GetImage`, `GetOsd`, `GetIsp`, and `GetEnc` fixtures for the target firmware and write `tests/camera/reolink-settings.test.ts` first so the bootstrap shape, section ids, and mixed setter strategy are pinned before the service is implemented.</action>
  <verify>
    <automated>npx vitest run tests/camera/reolink-settings.test.ts --testNamePattern "bootstrap|strategy"</automated>
  </verify>
  <done>The repo has one explicit settings contract file, firmware-backed read fixtures, and red/green tests that define the safe editable boundary and section ids before service code is added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the Reolink settings service with reread verification and setter-specific merge rules</name>
  <files>src/camera/reolink-settings.ts, tests/camera/reolink-settings.test.ts</files>
  <read_first>src/types/settings.ts, src/camera/reolink-session.ts, src/camera/capability-snapshot.ts, src/diagnostics/debug-capture.ts, tests/camera/reolink-settings.test.ts, tests/fixtures/reolink/get-time.json, tests/fixtures/reolink/get-ntp.json, tests/fixtures/reolink/get-image.json, tests/fixtures/reolink/get-osd.json, tests/fixtures/reolink/get-isp.json, tests/fixtures/reolink/get-enc.json, .planning/phases/04-settings-safe-configuration/04-RESEARCH.md</read_first>
  <acceptance_criteria>
    - `Test-Path src/camera/reolink-settings.ts` succeeds and `Select-String -Path src/camera/reolink-settings.ts -Pattern "GetTime|GetNtp|GetImage|GetOsd|GetIsp|GetEnc|SetTime|SetNtp|SetImage|SetOsd|SetEnc"` returns matches.
    - `Select-String -Path src/camera/reolink-settings.ts -Pattern "verified|changedFields|writeDebugArtifact|rspCode"` returns matches showing reread verification, diff output, debug capture, and camera error mapping are implemented.
    - `Select-String -Path tests/camera/reolink-settings.test.ts -Pattern "full-object|patch|no camera change detected|-4|-6|-9"` returns matches for strategy and reject-path coverage.
    - `npx vitest run tests/camera/reolink-settings.test.ts` passes.
  </acceptance_criteria>
  <behavior>
    - Test 1: Successful applies return a verified before/after summary built from a fresh camera reread per D-05 and D-06.
    - Test 2: `image` and `stream` sections reread current state, merge the draft into the full object, and reject partial write shortcuts.
    - Test 3: Camera rejects map to actionable section errors, and client-side mappable cases expose field errors where possible per D-07 and D-08.
  </behavior>
  <action>Implement `src/camera/reolink-settings.ts` with `createReolinkSettingsService()` that loads local config, loads the capability snapshot, and reuses `ReolinkSession` for all settings reads and writes. Per D-01, D-02, D-09, and D-10, `getBootstrap()` must combine `GetTime`, `GetNtp`, `GetImage`, `GetOsd`, `GetEnc`, and read-only `GetIsp` into normalized sections while omitting unsafe editable categories like network, storage, and alarms. Per D-05, every successful `applySection(sectionId, draft)` must re-read the written section and return verified `before`, `after`, and `changedFields`; for `time`, verify only static fields (`timeZone`, `timeFmt`, `hourFmt`, `Dst.*`) rather than advancing clock values. Per the research and D-05, maintain a hard setter matrix: `osd`, `ntp`, and `time` send narrowed patch-friendly payloads through `SetOsd`, `SetNtp`, and `SetTime`; `image` and `stream` must first read the current `Image` or `Enc` object, merge the draft into the full object, send `SetImage` or `SetEnc`, then re-read and diff the result. Map `rspCode -4`, `-6`, and `-9` into `SettingsApplyFailure` objects with section-level messages, and return field-specific errors only when the failing constraint is deterministically tied to one input. When `debugCapture` is enabled, emit sanitized artifacts for malformed reads or unexpected camera rejects, not for routine validation failures.</action>
  <verify>
    <automated>npx vitest run tests/camera/reolink-settings.test.ts -x</automated>
  </verify>
  <done>The backend now has one tested settings service that normalizes the safe Phase 4 subset, applies drafts with the right setter strategy, and verifies every successful write against a fresh camera reread.</done>
</task>

</tasks>

<verification>
- [ ] `npx vitest run tests/camera/reolink-settings.test.ts -x` succeeds
- [ ] `src/types/settings.ts` exports the shared settings DTOs, section ids, and service interface
- [ ] `src/camera/reolink-settings.ts` is the only place where raw `Get*` and `Set*` settings command names appear
</verification>

<success_criteria>
- Phase 4 has a concrete backend settings contract for routes and UI work
- The safe editable subset is explicit and capability-aware
- Verified rereads and mixed setter strategies are enforced before any browser-facing code is added
</success_criteria>

<output>
After completion, create `.planning/phases/04-settings-safe-configuration/04-01-SUMMARY.md`
</output>
