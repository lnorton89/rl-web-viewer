---
phase: 04-settings-safe-configuration
plan: 03
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - web/src/lib/settings-api.ts
  - web/src/hooks/use-settings.ts
  - web/src/components/SettingsPanel.tsx
  - web/src/components/SettingsSectionCard.tsx
  - web/src/components/SettingsReviewCallout.tsx
  - web/src/App.tsx
  - web/src/styles.css
  - tests/web/settings-panel.test.tsx
  - tests/web/live-view-controls.test.tsx
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-04
must_haves:
  truths:
    - "The dashboard shows the current safe settings subset and the wider inspect-only camera tuning section in one viewer-first layout."
    - "Each editable section stages its own draft, requires an inline review step, and applies changes independently from other sections."
    - "After a successful apply, the section shows a verified before/after summary; after a failed apply, the same section shows precise field or section feedback without losing the draft."
  artifacts:
    - path: "web/src/hooks/use-settings.ts"
      provides: "Section-scoped browser state machine for bootstrap, editing, review, apply, verify, and error states"
      contains: "Review Changes"
    - path: "web/src/components/SettingsPanel.tsx"
      provides: "Viewer-first settings surface with editable and inspect-only sections"
      contains: "Time & Sync"
    - path: "tests/web/settings-panel.test.tsx"
      provides: "jsdom coverage for section drafts, review/apply flow, verified summaries, and inline field errors"
      contains: "Verified against camera"
  key_links:
    - from: "web/src/lib/settings-api.ts"
      to: "src/server/routes/settings.ts"
      via: "all browser settings fetches and applies go through the Fastify settings API"
      pattern: "/api/settings"
    - from: "web/src/App.tsx"
      to: "web/src/components/SettingsPanel.tsx"
      via: "the viewer dashboard mounts the settings surface below the mode switcher and above diagnostics"
      pattern: "SettingsPanel"
    - from: "web/src/components/SettingsSectionCard.tsx"
      to: "web/src/hooks/use-settings.ts"
      via: "section badges, review/apply state, and verified/error messaging are driven by hook state"
      pattern: "review|verified|fieldErrors"
---

<objective>
Build the browser settings client, hook, and UI so Phase 4 delivers section-scoped review/apply/verified settings management inside the existing viewer-first dashboard.

Purpose: Turn the backend settings control plane into a trustworthy settings UX that is explicit about what changed, what is editable, and what the camera actually confirmed.
Output: A mounted `SettingsPanel`, section cards and review callouts, browser settings state management, updated dashboard styling, and jsdom coverage for the full review/apply/verify flow.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-settings-safe-configuration/04-CONTEXT.md
@.planning/phases/04-settings-safe-configuration/04-RESEARCH.md
@.planning/phases/04-settings-safe-configuration/04-UI-SPEC.md
@.planning/phases/04-settings-safe-configuration/04-VALIDATION.md
@.planning/phases/03-ptz-control-surface/03-03-SUMMARY.md
@web/src/App.tsx
@web/src/styles.css
@web/src/components/PtzPanel.tsx

<interfaces>
HTTP contract from plan 04-02:
```ts
GET /api/settings -> SettingsBootstrap
POST /api/settings/:sectionId/apply -> SettingsApplySuccess | SettingsApplyFailure
```

Phase 4 section order from 04-UI-SPEC:
```text
1. Time & Sync
2. Display Overlay
3. Basic Image Tuning
4. Stream Profile
5. Camera Tuning
```

Exact required state machine from 04-UI-SPEC:
```text
Read -> Editing -> Review -> Applying -> Verified
Read -> Editing -> Error
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the browser settings API client and section-scoped review/apply hook</name>
  <files>web/src/lib/settings-api.ts, web/src/hooks/use-settings.ts, tests/web/settings-panel.test.tsx</files>
  <read_first>src/types/settings.ts, src/server/routes/settings.ts, web/src/lib/ptz-api.ts, web/src/hooks/use-live-view.ts, tests/web/ptz-controls.test.tsx, .planning/phases/04-settings-safe-configuration/04-UI-SPEC.md, .planning/phases/04-settings-safe-configuration/04-RESEARCH.md</read_first>
  <acceptance_criteria>
    - `Test-Path web/src/lib/settings-api.ts` and `Test-Path web/src/hooks/use-settings.ts` both succeed.
    - `Select-String -Path web/src/lib/settings-api.ts -Pattern "/api/settings|/apply"` returns matches for bootstrap and apply calls.
    - `Select-String -Path web/src/hooks/use-settings.ts -Pattern "review|verified|fieldErrors|sectionError|aria-live"` returns matches showing section-state, feedback handling, and accessible status support are implemented.
    - `Select-String -Path tests/web/settings-panel.test.tsx -Pattern "Review Changes|Apply Settings|Verified against camera|Fix and Review Again"` returns matches for the key user flow.
    - `npx vitest run tests/web/settings-panel.test.tsx -x` passes.
  </acceptance_criteria>
  <behavior>
    - Test 1: Each section owns its own draft, diff, review, apply, verified, and error state per D-03 and D-04.
    - Test 2: Successful applies replace the review state with a verified summary from the server reread per D-05 and D-06.
    - Test 3: Field errors stay attached to the relevant inputs while section-level rejections keep the draft intact per D-07 and D-08.
  </behavior>
  <action>Create `web/src/lib/settings-api.ts` as the only browser client for `GET /api/settings` and `POST /api/settings/:sectionId/apply`. Implement `useSettings()` in `web/src/hooks/use-settings.ts` so it fetches bootstrap data on mount, stores section-local draft state keyed by `time`, `osd`, `image`, and `stream`, computes compact diff rows for inline review, and drives the exact Phase 4 state machine from the UI spec: `read`, `editing`, `review`, `applying`, `verified`, and `error`. Per D-03 and D-04, the hook must never mix drafts across sections and must require a section-local review state before calling `apply`. Per D-05 and D-06, successful applies must replace the draft review with the server-returned verified before/after summary. Per D-07 and D-08, store `fieldErrors` by input path and `sectionError` separately so inputs can render inline messages while broader rejections stay in a section-level panel. Add jsdom tests that mock the settings API and prove section-local drafts, disabled `Review Changes` with no diff, verified summaries, stream warning handling, and preserved drafts after failed applies.</action>
  <verify>
    <automated>npx vitest run tests/web/settings-panel.test.tsx -x</automated>
  </verify>
  <done>The browser has a dedicated settings hook and API client that enforce section-scoped review/apply behavior and preserve precise feedback state before any visual panel wiring is added.</done>
</task>

<task type="auto">
  <name>Task 2: Mount the settings panel in App with the approved section layout, copy, and status treatments</name>
  <files>web/src/components/SettingsPanel.tsx, web/src/components/SettingsSectionCard.tsx, web/src/components/SettingsReviewCallout.tsx, web/src/App.tsx, web/src/styles.css, tests/web/settings-panel.test.tsx, tests/web/live-view-controls.test.tsx</files>
  <read_first>web/src/App.tsx, web/src/styles.css, web/src/components/PtzPanel.tsx, web/src/components/DiagnosticsDisclosure.tsx, web/src/hooks/use-settings.ts, tests/web/settings-panel.test.tsx, tests/web/live-view-controls.test.tsx, .planning/phases/04-settings-safe-configuration/04-UI-SPEC.md</read_first>
  <acceptance_criteria>
    - `Select-String -Path web/src/App.tsx -Pattern "SettingsPanel"` returns a match showing the new settings surface is mounted between `ModeSwitcher` and `DiagnosticsDisclosure`.
    - `Select-String -Path web/src/components/SettingsPanel.tsx -Pattern "Time & Sync|Display Overlay|Basic Image Tuning|Stream Profile|Camera Tuning|No settings available"` returns matches for the exact section order and empty-state copy.
    - `Select-String -Path web/src/components/SettingsSectionCard.tsx -Pattern "Review Changes|Apply Settings|Back to Editing|Edit Again|Read only"` returns matches for the required action labels and badges.
    - `Select-String -Path web/src/styles.css -Pattern "settings-panel|settings-section-card|settings-review-callout|44px|980px"` returns matches for the new layout and control sizing hooks.
    - `npx vitest run tests/web/settings-panel.test.tsx tests/web/live-view-controls.test.tsx -x` passes.
    - `npm run build:web` passes.
  </acceptance_criteria>
  <action>Build `web/src/components/SettingsPanel.tsx`, `SettingsSectionCard.tsx`, and `SettingsReviewCallout.tsx` to match the Phase 4 UI contract exactly. Per D-01, D-02, D-09, and D-10, render the sections in this exact order: `Time & Sync`, `Display Overlay`, `Basic Image Tuning`, `Stream Profile`, and read-only `Camera Tuning`; keep network, storage, alarm, and unsupported categories out of the editable UI. Per D-03 and D-04, each editable section card must own its own `Edit`, `Cancel`, `Review Changes`, `Back to Editing`, and `Apply Settings` actions, with inline review content instead of a modal. Per D-05 and D-06, replace the review callout with a compact `Verified against camera` summary after success, including `No camera change detected` when the verified reread matches the original values. Per D-07 and D-08, render field errors directly under the input, a section-level rejection panel above section actions, and one `role="status"` / `aria-live="polite"` region per card so apply outcomes are announced. In `web/src/App.tsx`, mount `SettingsPanel` below `ModeSwitcher` and above `DiagnosticsDisclosure`, preserving the viewer/PTZ cluster and overall section order from the UI spec. Update `web/src/styles.css` using the existing custom CSS language: 24px card padding, 12px radii, 44px minimum control height, stacked section cards, accent-tinted verified state, muted read-only rows, and the stream warning copy `Applying stream changes may briefly reset live playback.`. If existing App-level live-view tests become coupled to the new panel mount, isolate them in `tests/web/live-view-controls.test.tsx` the same way Phase 3 isolated PTZ behavior.</action>
  <verify>
    <automated>npm run build:web && npx vitest run tests/web/settings-panel.test.tsx tests/web/live-view-controls.test.tsx -x</automated>
  </verify>
  <done>The dashboard now includes the approved settings surface with section-scoped review/apply/verified UX, inspect-only camera tuning, and test coverage for the critical user-facing flows.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build:web` succeeds
- [ ] `npx vitest run tests/web/settings-panel.test.tsx tests/web/live-view-controls.test.tsx -x` succeeds
- [ ] Against the live camera, confirm `Time & Sync`, `Display Overlay`, `Basic Image Tuning`, and `Stream Profile` render as editable while `Camera Tuning` renders read-only
- [ ] Against the live camera, change one safe field in each editable section and confirm each section returns a verified before/after summary after apply
- [ ] Trigger one invalid input path and confirm the UI shows either an inline field error or a section-level rejection panel without clearing the draft
</verification>

<success_criteria>
- The viewer-first dashboard now includes a trustworthy settings surface below transport controls and above diagnostics
- Section-local drafts, review/apply steps, and verified summaries follow the approved UI contract
- Precise error handling and reread-backed confirmation satisfy the Phase 4 user-visible requirements
</success_criteria>

<output>
After completion, create `.planning/phases/04-settings-safe-configuration/04-03-SUMMARY.md`
</output>
