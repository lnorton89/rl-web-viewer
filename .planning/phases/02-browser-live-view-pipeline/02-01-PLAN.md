---
phase: 02-browser-live-view-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - web/vite.config.ts
  - web/tsconfig.json
  - web/index.html
  - tests/web/browser-workspace.test.tsx
  - src/types/live-view.ts
  - src/media/live-view-modes.ts
  - tests/media/live-mode-selection.test.ts
autonomous: true
requirements:
  - LIVE-01
  - LIVE-02
user_setup: []
must_haves:
  truths:
    - "The app can derive browser-safe live-view modes from the persisted capability snapshot before any UI is rendered"
    - "The backend and frontend share typed browser playback endpoints instead of reconstructing WHEP, HLS, or snapshot URLs ad hoc"
    - "Phase 2 can install, typecheck, smoke-test, and build the browser workspace required for live view before backend and UI plans branch"
  artifacts:
    - path: "package.json"
      provides: "Fastify, React, Vite, hls.js, and browser-test scripts for the live-view stack"
      contains: "\"build:web\": \"vite build --config web/vite.config.ts\""
    - path: "src/types/live-view.ts"
      provides: "Shared live-view contracts used by backend and frontend, including browser-safe playback endpoints"
      contains: "export type LiveViewPlayback"
    - path: "src/media/live-view-modes.ts"
      provides: "Capability-gated mode matrix and startup/fallback ordering"
      contains: "DEFAULT_MODE_ORDER"
  key_links:
    - from: "src/camera/capability-snapshot.ts"
      to: "src/media/live-view-modes.ts"
      via: "normalized capability flags mapped into enabled live-view modes"
      pattern: "supportsLiveView|supportsSnapshot"
---

<objective>
Bootstrap the browser live-view workspace and create the shared contracts that both the server and viewer will implement.

Purpose: Lock the transport matrix, mode IDs, and toolchain up front so the remaining Phase 2 plans can execute in parallel without redefining interfaces.
Output: React/Vite/Fastify-capable package setup, browser-test configuration, and the shared live-view mode contract with fallback-selection tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-browser-live-view-pipeline/02-CONTEXT.md
@.planning/phases/02-browser-live-view-pipeline/02-RESEARCH.md
@.planning/phases/02-browser-live-view-pipeline/02-UI-SPEC.md
@.planning/phases/02-browser-live-view-pipeline/02-VALIDATION.md
@AGENTS.md
@package.json
@vitest.config.ts
@src/camera/capability-snapshot.ts

<interfaces>
From src/camera/capability-snapshot.ts:
```ts
export type CapabilitySnapshot = {
  identity: CameraIdentity;
  ports: {
    http: number;
    https: number;
    media: number;
    onvif: number;
    rtsp: number;
  };
  supportsLiveView: boolean;
  supportsPtzControl: boolean;
  supportsPtzPreset: boolean;
  supportsPtzPatrol: boolean;
  supportsSnapshot: boolean;
  supportsConfigRead: boolean;
};
```

Plan 02 must create this contract in src/types/live-view.ts:
```ts
export type StreamQuality = "main" | "sub";
export type StreamTransport = "webrtc" | "hls" | "snapshot";
export type LiveViewPlayback = {
  whepUrl: string | null;
  hlsUrl: string | null;
  snapshotUrl: string | null;
};
export type LiveModeId =
  | "webrtc:main"
  | "webrtc:sub"
  | "hls:main"
  | "hls:sub"
  | "snapshot:main"
  | "snapshot:sub";
export type LiveMode = {
  id: LiveModeId;
  playback: LiveViewPlayback;
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the browser live-view toolchain and dual-environment test setup</name>
  <files>package.json, vitest.config.ts, web/vite.config.ts, web/tsconfig.json, web/index.html, tests/web/browser-workspace.test.tsx</files>
  <read_first>package.json, vitest.config.ts, web/vite.config.ts, web/tsconfig.json, web/index.html, tests/web/browser-workspace.test.tsx, .planning/phases/02-browser-live-view-pipeline/02-CONTEXT.md, .planning/phases/02-browser-live-view-pipeline/02-RESEARCH.md, .planning/phases/02-browser-live-view-pipeline/02-UI-SPEC.md</read_first>
  <action>Update `package.json` to add runtime dependencies `fastify`, `@fastify/static`, `react`, `react-dom`, and `hls.js`, plus dev dependencies `vite`, `@vitejs/plugin-react`, `jsdom`, `@testing-library/react`, and `@testing-library/user-event`, per D-01 and D-02. Keep the existing `probe` script intact for Phase 1, and set scripts to exact values: `"build": "npm run build:server && npm run build:web"`, `"build:server": "tsc -p tsconfig.json"`, `"build:web": "vite build --config web/vite.config.ts"`, `"dev:web": "vite --config web/vite.config.ts"`, and `"test": "vitest run"`. Update `vitest.config.ts` so `tests/web/**/*.test.tsx` runs in `jsdom` while all other tests keep the `node` environment. Create `web/vite.config.ts` using `@vitejs/plugin-react`, `root: "."`, `server.host: "127.0.0.1"`, `server.port: 5173`, and `build.outDir: "dist"` with `manifest: true`. Create `web/tsconfig.json` extending the repo root config and create `web/index.html` with a single `<div id="app"></div>` mount point. Add `tests/web/browser-workspace.test.tsx` as the Wave 0 browser-workspace smoke check: it must run under the `jsdom` split from `vitest.config.ts`, assert `web/index.html` exposes the `app` mount, and import `web/vite.config.ts` to verify the planned host, port, output directory, and manifest settings so Task 1 validates the new browser workspace itself instead of only reusing Phase 1 coverage.</action>
  <verify>
    <automated>npx tsc -p web/tsconfig.json --noEmit && npx vitest run tests/web/browser-workspace.test.tsx -x && npx vite build --config web/vite.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"build:web": "vite build --config web/vite.config.ts"`
    - package.json contains `"dev:web": "vite --config web/vite.config.ts"`
    - package.json contains `"@fastify/static"`
    - package.json contains `"react-dom"`
    - vitest.config.ts contains `tests/web/**/*.test.tsx`
    - vitest.config.ts contains `jsdom`
    - web/vite.config.ts contains `server: {`
    - web/vite.config.ts contains `port: 5173`
    - web/index.html contains `<div id="app"></div>`
    - tests/web/browser-workspace.test.tsx contains `web/index.html`
    - tests/web/browser-workspace.test.tsx contains `web/vite.config.ts`
  </acceptance_criteria>
  <done>The repo can typecheck, smoke-test, and build the new browser workspace without replacing the Phase 1 probe path.</done>
</task>

<task type="auto">
  <name>Task 2: Define the shared live-view contract and capability-driven mode matrix</name>
  <files>src/types/live-view.ts, src/media/live-view-modes.ts, tests/media/live-mode-selection.test.ts</files>
  <read_first>src/types/live-view.ts, src/media/live-view-modes.ts, tests/media/live-mode-selection.test.ts, src/camera/capability-snapshot.ts, .planning/phases/02-browser-live-view-pipeline/02-CONTEXT.md, .planning/phases/02-browser-live-view-pipeline/02-RESEARCH.md, .planning/phases/02-browser-live-view-pipeline/02-UI-SPEC.md</read_first>
  <action>Create `src/types/live-view.ts` with exact exports `StreamQuality`, `StreamTransport`, `LiveModeId`, `ViewerStateKind`, `LiveViewPlayback`, `LiveMode`, `LiveViewBootstrap`, and `LiveViewDiagnostics`. `LiveViewPlayback` must expose exact nullable browser endpoint fields `whepUrl`, `hlsUrl`, and `snapshotUrl`, and every `LiveMode` must carry a `playback` object so the backend returns browser-safe URLs and the frontend consumes them without reconstructing paths. Use exact mode IDs `webrtc:main`, `webrtc:sub`, `hls:main`, `hls:sub`, `snapshot:main`, and `snapshot:sub`. Create `src/media/live-view-modes.ts` with `DEFAULT_MODE_ORDER = ["webrtc:main", "webrtc:sub", "hls:sub", "snapshot:main"]`, `buildLiveModes(snapshot: CapabilitySnapshot): LiveMode[]`, `pickPreferredMode(modes)`, and `buildFallbackOrder(modes)`. Enable `webrtc:*` and `hls:*` only when `snapshot.supportsLiveView === true` and `snapshot.ports.rtsp > 0`; enable `snapshot:*` only when `snapshot.supportsSnapshot === true`. Initialize disabled modes with all playback fields `null` until the server fills them later. Labels must be exact human-facing values `WebRTC Main`, `WebRTC Sub`, `HLS Main`, `HLS Sub`, `Snapshot Main`, and `Snapshot Sub`, per D-03, D-04, and the UI contract. Create `tests/media/live-mode-selection.test.ts` covering startup order `webrtc:main -> webrtc:sub -> hls:sub -> snapshot:main`, snapshot-disabled behavior, and no-live-view behavior when `supportsLiveView` is false.</action>
  <verify>
    <automated>npx vitest run tests/media/live-mode-selection.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - src/types/live-view.ts contains `export type LiveModeId`
    - src/types/live-view.ts contains `whepUrl`
    - src/types/live-view.ts contains `hlsUrl`
    - src/types/live-view.ts contains `snapshotUrl`
    - src/types/live-view.ts contains `"snapshot:sub"`
    - src/media/live-view-modes.ts contains `DEFAULT_MODE_ORDER`
    - src/media/live-view-modes.ts contains `WebRTC Main`
    - src/media/live-view-modes.ts contains `supportsSnapshot`
    - tests/media/live-mode-selection.test.ts contains `webrtc:main`
    - tests/media/live-mode-selection.test.ts contains `snapshot:main`
    - tests/media/live-mode-selection.test.ts contains `supportsLiveView`
  </acceptance_criteria>
  <done>The backend and frontend share one exact mode contract and one exact startup/fallback ladder before implementation diverges.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds
- [ ] `npx vitest run tests/media/live-mode-selection.test.ts -x` succeeds
- [ ] The mode contract exposes transport, quality, and browser playback endpoints separately rather than one combined opaque selector
</verification>

<success_criteria>
- The repo has the exact browser stack dependencies required by the research recommendation
- Shared live-view contracts exist in code, not only in planning docs
- The startup/fallback ladder is encoded and tested before server or viewer implementation begins
</success_criteria>

<output>
After completion, create `.planning/phases/02-browser-live-view-pipeline/02-01-SUMMARY.md`
</output>
