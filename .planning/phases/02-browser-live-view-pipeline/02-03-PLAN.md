---
phase: 02-browser-live-view-pipeline
plan: 03
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - web/src/main.tsx
  - web/src/App.tsx
  - web/src/styles.css
  - web/src/hooks/use-live-view.ts
  - web/src/lib/live-view-api.ts
  - web/src/lib/players/mediamtx-webrtc-reader.ts
  - web/src/lib/players/webrtc-player.ts
  - web/src/lib/players/hls-player.ts
  - web/src/lib/players/snapshot-player.ts
  - web/src/components/LiveViewerFrame.tsx
  - web/src/components/ViewerStatusOverlay.tsx
  - web/src/components/ModeSwitcher.tsx
  - web/src/components/DiagnosticsDisclosure.tsx
  - tests/web/live-view-viewer.test.tsx
  - tests/web/live-view-controls.test.tsx
autonomous: true
requirements:
  - LIVE-01
  - LIVE-02
  - LIVE-03
user_setup: []
must_haves:
  truths:
    - "The dashboard auto-starts a browser-safe live mode when the page opens"
    - "The user can switch between supported stream qualities and fallback modes after startup"
    - "The viewer shows connecting, live, reconnecting, and failed states in-frame with retry and short reasons"
  artifacts:
    - path: "web/src/hooks/use-live-view.ts"
      provides: "Viewer state machine, retry policy, and fallback ladder"
      contains: "reconnecting"
    - path: "web/src/components/LiveViewerFrame.tsx"
      provides: "Primary viewer surface and overlay composition"
      contains: "Retry Live View"
    - path: "tests/web/live-view-viewer.test.tsx"
      provides: "UI verification for LIVE-03"
      contains: "Live View Failed"
  key_links:
    - from: "web/src/hooks/use-live-view.ts"
      to: "/api/live-view"
      via: "bootstrap fetch on mount"
      pattern: "/api/live-view"
    - from: "web/src/components/ModeSwitcher.tsx"
      to: "web/src/hooks/use-live-view.ts"
      via: "manual mode selection stays sticky until definitive failure"
      pattern: "selectMode"
---

<objective>
Build the React live viewer that auto-starts the preferred mode, supports manual switching, and surfaces transport state inside the frame.

Purpose: Turn the Phase 2 backend into the modern Flash-free live-view experience the user actually interacts with.
Output: React viewer app, transport adapters for WebRTC/HLS/snapshot, UI components matching the approved design contract, and jsdom coverage for the viewer lifecycle.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-browser-live-view-pipeline/02-CONTEXT.md
@.planning/phases/02-browser-live-view-pipeline/02-RESEARCH.md
@.planning/phases/02-browser-live-view-pipeline/02-UI-SPEC.md
@.planning/phases/02-browser-live-view-pipeline/02-VALIDATION.md
@.planning/phases/02-browser-live-view-pipeline/02-01-SUMMARY.md
@src/types/live-view.ts
@web/index.html
@web/vite.config.ts

<interfaces>
From src/types/live-view.ts (created in 02-01):
```ts
export type ViewerStateKind = "connecting" | "live" | "reconnecting" | "failed";

export type LiveViewBootstrap = {
  preferredModeId: LiveModeId | null;
  fallbackOrder: LiveModeId[];
  modes: LiveMode[];
  diagnostics: LiveViewDiagnostics;
};
```

The UI must keep these exact labels from 02-UI-SPEC:
```text
Connecting
Live
Reconnecting
Live View Failed
Retry Live View
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the transport-aware viewer hook and playback adapters</name>
  <files>web/src/hooks/use-live-view.ts, web/src/lib/live-view-api.ts, web/src/lib/players/mediamtx-webrtc-reader.ts, web/src/lib/players/webrtc-player.ts, web/src/lib/players/hls-player.ts, web/src/lib/players/snapshot-player.ts, tests/web/live-view-viewer.test.tsx</files>
  <read_first>web/src/hooks/use-live-view.ts, web/src/lib/live-view-api.ts, web/src/lib/players/mediamtx-webrtc-reader.ts, web/src/lib/players/webrtc-player.ts, web/src/lib/players/hls-player.ts, web/src/lib/players/snapshot-player.ts, tests/web/live-view-viewer.test.tsx, src/types/live-view.ts, .planning/phases/02-browser-live-view-pipeline/02-CONTEXT.md, .planning/phases/02-browser-live-view-pipeline/02-UI-SPEC.md</read_first>
  <action>Create `web/src/lib/live-view-api.ts` with `fetchLiveViewBootstrap()` that requests `/api/live-view`. Create `web/src/lib/players/mediamtx-webrtc-reader.ts` by vendoring the official MediaMTX browser reader helper in a local file, then wrap it in `attachWebRtcPlayer(video, whepUrl)`. Create `web/src/lib/players/hls-player.ts` with `attachHlsPlayer(video, playlistUrl)` using `hls.js`, and `web/src/lib/players/snapshot-player.ts` with `attachSnapshotPlayer(img, snapshotUrl, refreshMs = 1000)` for the 1 fps fallback resolved in the UI spec. Build `web/src/hooks/use-live-view.ts` to fetch bootstrap data on mount per D-03 and D-05, auto-start the exact preferred mode returned by `/api/live-view`, model exact viewer states `connecting`, `live`, `reconnecting`, and `failed`, and apply reconnect delays `[1000, 2000, 5000]` before stepping down the fallback order from 02-01, per D-06. Manual selections must stay active until that specific mode fails definitively; they must not snap back to auto mode after a successful user switch. Create `tests/web/live-view-viewer.test.tsx` with jsdom tests covering initial `Connecting`, transition to `Live`, fallback into `Reconnecting`, and terminal `Live View Failed` plus the `Retry Live View` action.</action>
  <verify>
    <automated>npx vitest run tests/web/live-view-viewer.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - web/src/hooks/use-live-view.ts contains `/api/live-view`
    - web/src/hooks/use-live-view.ts contains `1000, 2000, 5000`
    - web/src/hooks/use-live-view.ts contains `reconnecting`
    - web/src/lib/players/snapshot-player.ts contains `refreshMs = 1000`
    - web/src/lib/players/hls-player.ts contains `Hls`
    - tests/web/live-view-viewer.test.tsx contains `Live View Failed`
    - tests/web/live-view-viewer.test.tsx contains `Retry Live View`
  </acceptance_criteria>
  <done>The viewer lifecycle is encoded in code and tests, not inferred from generic video-element behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Build the approved viewer UI, manual mode switcher, and diagnostics disclosure</name>
  <files>web/src/main.tsx, web/src/App.tsx, web/src/styles.css, web/src/components/LiveViewerFrame.tsx, web/src/components/ViewerStatusOverlay.tsx, web/src/components/ModeSwitcher.tsx, web/src/components/DiagnosticsDisclosure.tsx, tests/web/live-view-controls.test.tsx</files>
  <read_first>web/src/main.tsx, web/src/App.tsx, web/src/styles.css, web/src/components/LiveViewerFrame.tsx, web/src/components/ViewerStatusOverlay.tsx, web/src/components/ModeSwitcher.tsx, web/src/components/DiagnosticsDisclosure.tsx, tests/web/live-view-controls.test.tsx, .planning/phases/02-browser-live-view-pipeline/02-UI-SPEC.md, src/types/live-view.ts</read_first>
  <action>Create `web/src/main.tsx` and `web/src/App.tsx` to render a single viewer-first dashboard. Implement `LiveViewerFrame`, `ViewerStatusOverlay`, `ModeSwitcher`, and `DiagnosticsDisclosure` using the approved UI contract: IBM Plex Sans, dominant background `#12161B`, secondary surfaces `#1D242C`, accent `#63C7B2`, 12px radius, viewer frame that occupies at least `70vh` on desktop, segmented mode buttons with a `44px` minimum hit area, and a collapsed diagnostics panel by default. Overlay labels must be exact text `Connecting`, `Live`, `Reconnecting`, and `Live View Failed`, with a short reason line under 90 characters. `ModeSwitcher` labels must separate transport and quality, for example `WebRTC Main`, `HLS Sub`, and `Snapshot Main`, per D-04. `DiagnosticsDisclosure` must show current mode, last reason, and next fallback mode without exposing raw credentials or camera URLs. Add a polite live region that mirrors the visible overlay text. Create `tests/web/live-view-controls.test.tsx` to verify manual mode switching, diagnostics disclosure toggling, the exact `Retry Live View` label, and the presence of all required overlay labels.</action>
  <verify>
    <automated>npx vitest run tests/web/live-view-viewer.test.tsx tests/web/live-view-controls.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - web/src/styles.css contains `font-family: "IBM Plex Sans"`
    - web/src/styles.css contains `#12161B`
    - web/src/styles.css contains `min-height: 70vh`
    - web/src/components/ViewerStatusOverlay.tsx contains `Live View Failed`
    - web/src/components/ModeSwitcher.tsx contains `WebRTC Main`
    - web/src/components/DiagnosticsDisclosure.tsx contains `next fallback`
    - tests/web/live-view-controls.test.tsx contains `Retry Live View`
    - tests/web/live-view-controls.test.tsx contains `Diagnostics`
  </acceptance_criteria>
  <done>The browser UI matches the approved Phase 2 contract and gives the user explicit status, switching, and retry control inside the viewer.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build:web` succeeds
- [ ] `npx vitest run tests/web/live-view-viewer.test.tsx tests/web/live-view-controls.test.tsx -x` succeeds
- [ ] The viewer auto-starts a preferred mode, keeps manual mode selections sticky, and shows the exact approved overlay labels
</verification>

<success_criteria>
- The browser app auto-starts the preferred low-latency mode on page load
- Manual quality/fallback switching is visible and test-covered
- Connecting, live, reconnecting, and failed states are rendered in-frame with retry and short reasons
</success_criteria>

<output>
After completion, create `.planning/phases/02-browser-live-view-pipeline/02-03-SUMMARY.md`
</output>
