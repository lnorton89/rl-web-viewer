---
phase: 02-browser-live-view-pipeline
plan: 03
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - web/src/hooks/use-live-view.ts
  - web/src/lib/live-view-api.ts
  - web/src/lib/players/mediamtx-webrtc-reader.ts
  - web/src/lib/players/webrtc-player.ts
  - web/src/lib/players/hls-player.ts
  - web/src/lib/players/snapshot-player.ts
  - tests/web/live-view-viewer.test.tsx
autonomous: true
requirements:
  - LIVE-01
  - LIVE-03
user_setup: []
must_haves:
  truths:
    - "The dashboard auto-starts a browser-safe live mode when the page opens"
    - "The viewer state machine uses backend-provided playback endpoints rather than rebuilding URLs in the browser"
    - "The viewer shows connecting, live, reconnecting, and failed states with retry and short reasons before UI chrome is layered on top"
  artifacts:
    - path: "web/src/hooks/use-live-view.ts"
      provides: "Viewer state machine, retry policy, fallback ladder, and media-element binding contract for the frame"
      contains: "bindVideoElement"
    - path: "web/src/lib/live-view-api.ts"
      provides: "Browser bootstrap client for the shared live-view contract"
      contains: "fetchLiveViewBootstrap"
    - path: "tests/web/live-view-viewer.test.tsx"
      provides: "Viewer lifecycle and media-mount verification for LIVE-03"
      contains: "bindVideoElement"
  key_links:
    - from: "web/src/hooks/use-live-view.ts"
      to: "/api/live-view"
      via: "bootstrap fetch on mount"
      pattern: "/api/live-view"
    - from: "web/src/hooks/use-live-view.ts"
      to: "web/src/lib/players/webrtc-player.ts"
      via: "selected transport attaches the matching playback adapter using contract-provided endpoint fields"
      pattern: "attachWebRtcPlayer|attachHlsPlayer|attachSnapshotPlayer"
    - from: "web/src/hooks/use-live-view.ts"
      to: "web/src/components/LiveViewerFrame.tsx"
      via: "the hook exposes callback refs and the active render target so the frame can mount the correct media element for the selected player"
      pattern: "bindVideoElement|bindImageElement|renderKind"
---

<objective>
Build the transport-aware viewer state layer that consumes the shared live-view contract and drives playback adapters.

Purpose: Lock the browser playback and state-machine behavior before the visual viewer shell is composed in the follow-on UI plan.
Output: Bootstrap API client, WebRTC/HLS/snapshot adapters, the viewer hook/state machine, and jsdom coverage for the lifecycle and retry behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-browser-live-view-pipeline/02-CONTEXT.md
@.planning/phases/02-browser-live-view-pipeline/02-RESEARCH.md
@.planning/phases/02-browser-live-view-pipeline/02-UI-SPEC.md
@.planning/phases/02-browser-live-view-pipeline/02-VALIDATION.md
@.planning/phases/02-browser-live-view-pipeline/02-01-SUMMARY.md
@src/types/live-view.ts
@web/tsconfig.json

<interfaces>
From src/types/live-view.ts (created in 02-01):
```ts
export type ViewerStateKind = "connecting" | "live" | "reconnecting" | "failed";

export type LiveViewPlayback = {
  whepUrl: string | null;
  hlsUrl: string | null;
  snapshotUrl: string | null;
};

export type LiveMode = {
  id: LiveModeId;
  playback: LiveViewPlayback;
};

export type LiveViewBootstrap = {
  preferredModeId: LiveModeId | null;
  fallbackOrder: LiveModeId[];
  modes: LiveMode[];
  diagnostics: LiveViewDiagnostics;
};
```

Plan 03 must define the hook-to-frame media contract explicitly:
```ts
export function useLiveView(): {
  state: { kind: ViewerStateKind; reason?: string };
  modes: LiveMode[];
  currentModeId: LiveModeId | null;
  fallbackOrder: LiveModeId[];
  nextFallbackModeId: LiveModeId | null;
  renderKind: "video" | "image";
  activePlayback: LiveViewPlayback | null;
  bindVideoElement(node: HTMLVideoElement | null): void;
  bindImageElement(node: HTMLImageElement | null): void;
  selectMode(modeId: LiveModeId): Promise<void>;
  retry(): Promise<void>;
};
```

The UI must keep these exact labels from 02-UI-SPEC:
```text
Connecting
Live
Reconnecting
Live View Failed
Retry Live View
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the bootstrap client and transport adapters around the shared playback contract</name>
  <files>web/src/lib/live-view-api.ts, web/src/lib/players/mediamtx-webrtc-reader.ts, web/src/lib/players/webrtc-player.ts, web/src/lib/players/hls-player.ts, web/src/lib/players/snapshot-player.ts</files>
  <read_first>web/src/lib/live-view-api.ts, web/src/lib/players/mediamtx-webrtc-reader.ts, web/src/lib/players/webrtc-player.ts, web/src/lib/players/hls-player.ts, web/src/lib/players/snapshot-player.ts, src/types/live-view.ts, .planning/phases/02-browser-live-view-pipeline/02-CONTEXT.md, .planning/phases/02-browser-live-view-pipeline/02-RESEARCH.md</read_first>
  <action>Create `web/src/lib/live-view-api.ts` with `fetchLiveViewBootstrap()` that requests `/api/live-view` and returns the typed `LiveViewBootstrap` contract from 02-01 without remapping its playback fields. Create `web/src/lib/players/mediamtx-webrtc-reader.ts` by vendoring the official MediaMTX browser reader helper in a local file, then wrap it in `attachWebRtcPlayer(video, whepUrl)`. Create `web/src/lib/players/hls-player.ts` with `attachHlsPlayer(video, playlistUrl)` using `hls.js`, and `web/src/lib/players/snapshot-player.ts` with `attachSnapshotPlayer(img, snapshotUrl, refreshMs = 1000)` for the 1 fps fallback resolved in the UI spec. Player adapters must consume the exact backend-provided `playback.whepUrl`, `playback.hlsUrl`, and `playback.snapshotUrl` fields instead of constructing URLs in the browser.</action>
  <verify>
    <automated>npx tsc -p web/tsconfig.json --noEmit</automated>
  </verify>
  <done>The browser has transport adapters that are driven by the shared backend contract rather than implicit URL conventions.</done>
</task>

<task type="auto">
  <name>Task 2: Build the viewer hook state machine and lifecycle tests</name>
  <files>web/src/hooks/use-live-view.ts, tests/web/live-view-viewer.test.tsx</files>
  <read_first>web/src/hooks/use-live-view.ts, tests/web/live-view-viewer.test.tsx, web/src/lib/live-view-api.ts, web/src/lib/players/webrtc-player.ts, web/src/lib/players/hls-player.ts, web/src/lib/players/snapshot-player.ts, src/types/live-view.ts, .planning/phases/02-browser-live-view-pipeline/02-CONTEXT.md, .planning/phases/02-browser-live-view-pipeline/02-UI-SPEC.md</read_first>
  <action>Build `web/src/hooks/use-live-view.ts` to fetch bootstrap data on mount per D-03 and D-05, auto-start the exact preferred mode returned by `/api/live-view`, consume the mode's `playback` fields when attaching a player, model exact viewer states `connecting`, `live`, `reconnecting`, and `failed`, and apply reconnect delays `[1000, 2000, 5000]` before stepping down the fallback order from 02-01, per D-06. The hook must expose the exact fallback metadata needed by the UI: `fallbackOrder` from the bootstrap contract plus a derived `nextFallbackModeId` that resolves the next candidate mode after the active or failed mode. Define the exact hook-to-frame contract `renderKind`, `activePlayback`, `bindVideoElement(node)`, and `bindImageElement(node)` so `LiveViewerFrame` can mount either a `<video>` or `<img>` surface and hand the real DOM node back to the hook for adapter attachment. Keep manual selections sticky internally so the later UI plan can expose them without extra fallback logic; a user-selected mode must stay active until that specific mode fails definitively. Create `tests/web/live-view-viewer.test.tsx` with jsdom tests covering initial `Connecting`, transition to `Live`, fallback into `Reconnecting`, terminal `Live View Failed`, the `Retry Live View` action, adapter attachment through the callback-ref media binding contract, and the derived `nextFallbackModeId` metadata used by diagnostics.</action>
  <verify>
    <automated>npx vitest run tests/web/live-view-viewer.test.tsx -x</automated>
  </verify>
  <done>The viewer lifecycle is encoded in code and tests before the UI shell consumes it.</done>
</task>

</tasks>

<verification>
- [ ] `npx tsc -p web/tsconfig.json --noEmit` succeeds
- [ ] `npx vitest run tests/web/live-view-viewer.test.tsx -x` succeeds
- [ ] The viewer auto-starts a preferred mode, keeps manual mode selections sticky internally, exposes `fallbackOrder` plus `nextFallbackModeId`, and exposes the explicit media-binding contract `renderKind` plus callback refs used by `LiveViewerFrame`
</verification>

<success_criteria>
- The browser app auto-starts the preferred low-latency mode on page load
- Browser playback adapters consume server-provided WHEP/HLS/snapshot fields instead of inferring URLs locally
- Connecting, live, reconnecting, and failed states are encoded and test-covered before UI composition begins
</success_criteria>

<output>
After completion, create `.planning/phases/02-browser-live-view-pipeline/02-03-SUMMARY.md`
</output>
