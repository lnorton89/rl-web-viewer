---
phase: 02-browser-live-view-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/camera/reolink-live-streams.ts
  - src/media/mediamtx-runtime.ts
  - src/media/mediamtx-config.ts
  - src/media/live-view-service.ts
  - src/server/create-server.ts
  - src/server/routes/live-view.ts
  - src/index.ts
  - tests/media/mediamtx-runtime-config.test.ts
  - tests/media/live-view-errors.test.ts
  - tests/server/live-view-routes.test.ts
autonomous: true
requirements:
  - LIVE-01
  - LIVE-02
  - LIVE-03
user_setup: []
must_haves:
  truths:
    - "The browser can request live-view URLs and fallback metadata from the local app without receiving camera credentials"
    - "The app can bootstrap a preferred low-latency live mode and expose fallback/snapshot routes for later UI use"
    - "Live-view startup and bootstrap failures can be surfaced as short reasons and sanitized diagnostics"
    - "The app can start the correct live stream for the connected camera while keeping firmware-specific path selection inside the adapter layer"
  artifacts:
    - path: "src/camera/reolink-live-streams.ts"
      provides: "Firmware-specific stream path selection for the known Reolink adapter"
      contains: "h264Preview_01_main"
    - path: "tests/media/mediamtx-runtime-config.test.ts"
      provides: "Focused coverage for pinned runtime bootstrap and generic relay config generation"
      contains: "camera_main"
    - path: "src/media/live-view-service.ts"
      provides: "Capability-aware live-view bootstrap and failure classification"
      contains: "buildLiveViewBootstrap"
    - path: "src/server/routes/live-view.ts"
      provides: "Live-view JSON bootstrap and snapshot proxy routes"
      contains: "/api/live-view"
    - path: "tests/media/live-view-errors.test.ts"
      provides: "Media-layer coverage for bootstrap failure classification and LIVE-03 Nyquist requirements"
      contains: "classifyLiveViewFailure"
  key_links:
    - from: "src/camera/reolink-live-streams.ts"
      to: "src/media/mediamtx-config.ts"
      via: "adapter-resolved camera stream sources mapped onto generic app relay paths"
      pattern: "camera_main|camera_sub"
    - from: "src/server/routes/live-view.ts"
      to: "src/media/live-view-service.ts"
      via: "bootstrap JSON returned from the Fastify route"
      pattern: "buildLiveViewBootstrap"
---

<objective>
Implement the MediaMTX-backed live-view control plane and the Fastify routes that keep camera credentials inside the Node app.

Purpose: Deliver the actual browser-safe live-view backend described by the research and keep the transport boundary app-owned instead of camera-owned.
Output: Pinned MediaMTX runtime bootstrap, capability-aware live-view service, Fastify API routes, and route tests that guard credential leakage and failure messaging.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-browser-live-view-pipeline/02-CONTEXT.md
@.planning/phases/02-browser-live-view-pipeline/02-RESEARCH.md
@.planning/phases/02-browser-live-view-pipeline/02-UI-SPEC.md
@.planning/phases/02-browser-live-view-pipeline/02-VALIDATION.md
@.planning/phases/02-browser-live-view-pipeline/02-01-SUMMARY.md
@src/config/camera-config.ts
@src/camera/reolink-session.ts
@src/camera/capability-snapshot.ts
@src/camera/reolink-live-streams.ts
@src/diagnostics/debug-capture.ts
@src/types/live-view.ts
@src/media/live-view-modes.ts

<interfaces>
From src/types/live-view.ts (created in 02-01):
```ts
export type LiveViewPlayback = {
  whepUrl: string | null;
  hlsUrl: string | null;
  snapshotUrl: string | null;
};

export type LiveMode = {
  id: LiveModeId;
  label: string;
  quality: StreamQuality;
  transport: StreamTransport;
  enabled: boolean;
  playback: LiveViewPlayback;
  disabledReason?: string;
};

export type LiveViewBootstrap = {
  preferredModeId: LiveModeId | null;
  fallbackOrder: LiveModeId[];
  modes: LiveMode[];
  diagnostics: LiveViewDiagnostics;
};
```

From src/camera/capability-snapshot.ts:
```ts
export type CapabilitySnapshot = {
  supportsLiveView: boolean;
  supportsSnapshot: boolean;
  ports: { rtsp: number };
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the pinned MediaMTX runtime, adapter-backed stream mapping, and live-view bootstrap service</name>
  <files>src/camera/reolink-live-streams.ts, src/media/mediamtx-runtime.ts, src/media/mediamtx-config.ts, src/media/live-view-service.ts, tests/media/mediamtx-runtime-config.test.ts, tests/media/live-view-errors.test.ts</files>
  <read_first>src/camera/reolink-live-streams.ts, src/media/mediamtx-runtime.ts, src/media/mediamtx-config.ts, src/media/live-view-service.ts, tests/media/mediamtx-runtime-config.test.ts, tests/media/live-view-errors.test.ts, src/config/camera-config.ts, src/camera/capability-snapshot.ts, src/diagnostics/debug-capture.ts, src/types/live-view.ts, src/media/live-view-modes.ts, .planning/phases/02-browser-live-view-pipeline/02-RESEARCH.md</read_first>
  <action>Create `src/media/mediamtx-runtime.ts` that ensures a pinned Windows runtime at `.local/tools/mediamtx-v1.17.1/mediamtx.exe`, downloading `mediamtx_v1.17.1_windows_amd64.zip` from the official GitHub release if the executable is missing. Create `src/camera/reolink-live-streams.ts` as the camera-specific adapter helper that resolves the known RLC-423S RTSP suffixes `h264Preview_01_main` and `h264Preview_01_sub` from the persisted capability snapshot and camera config; keep these firmware-specific strings out of the generic media layer per the project adapter constraint. Create `src/media/mediamtx-config.ts` so it only owns the generic relay shape: write `.local/runtime/mediamtx.yml` with exact app-facing paths `camera_main` and `camera_sub`, accept adapter-resolved RTSP source URLs, and set `sourceOnDemand: yes` on both paths. Create `src/media/live-view-service.ts` exporting `buildLiveViewBootstrap(requestHost: string)`, `startMediaRelay()`, and `classifyLiveViewFailure(error)`; `buildLiveViewBootstrap()` must load the persisted capability snapshot, call `buildLiveModes()` and `pickPreferredMode()`, fill each enabled mode's `playback.whepUrl`, `playback.hlsUrl`, and `playback.snapshotUrl` fields with app-owned browser URLs that use the request hostname with exact MediaMTX ports `8889` for `/camera_main/whep` and `/camera_sub/whep`, `8888` for `/camera_main/index.m3u8` and `/camera_sub/index.m3u8`, plus relative snapshot URLs `/api/live-view/snapshot/main` and `/api/live-view/snapshot/sub`. When no supported mode exists or a bootstrap step fails, write a sanitized debug artifact with `command: "live-view-bootstrap"` and include a short `reason` string per D-07 and D-08. Create `tests/media/mediamtx-runtime-config.test.ts` covering pinned runtime path resolution, release ZIP naming, and `camera_main` / `camera_sub` config generation from adapter-resolved sources. Create `tests/media/live-view-errors.test.ts` covering `classifyLiveViewFailure(error)`, no-supported-mode bootstrap behavior, and the presence of browser-safe playback fields in enabled modes.</action>
  <verify>
    <automated>npx vitest run tests/media/mediamtx-runtime-config.test.ts tests/media/live-view-errors.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - src/camera/reolink-live-streams.ts contains `h264Preview_01_main`
    - src/camera/reolink-live-streams.ts contains `h264Preview_01_sub`
    - src/media/mediamtx-runtime.ts contains `mediamtx-v1.17.1`
    - src/media/mediamtx-runtime.ts contains `mediamtx_v1.17.1_windows_amd64.zip`
    - src/media/mediamtx-config.ts contains `camera_main`
    - src/media/mediamtx-config.ts contains `sourceOnDemand: yes`
    - src/media/live-view-service.ts contains `buildLiveViewBootstrap`
    - src/media/live-view-service.ts contains `whepUrl`
    - src/media/live-view-service.ts contains `hlsUrl`
    - src/media/live-view-service.ts contains `snapshotUrl`
    - src/media/live-view-service.ts contains `/api/live-view/snapshot/main`
    - src/media/live-view-service.ts contains `live-view-bootstrap`
    - tests/media/mediamtx-runtime-config.test.ts contains `camera_main`
    - tests/media/mediamtx-runtime-config.test.ts contains `mediamtx_v1.17.1_windows_amd64.zip`
    - tests/media/live-view-errors.test.ts contains `classifyLiveViewFailure`
  </acceptance_criteria>
  <done>The local app can derive a browser-safe bootstrap payload and a pinned relay runtime from the Phase 1 camera config and capability snapshot.</done>
</task>

<task type="auto">
  <name>Task 2: Expose the live-view bootstrap, snapshot proxy, and static dashboard routes through Fastify</name>
  <files>src/server/create-server.ts, src/server/routes/live-view.ts, src/index.ts, tests/server/live-view-routes.test.ts</files>
  <read_first>src/server/create-server.ts, src/server/routes/live-view.ts, src/index.ts, tests/server/live-view-routes.test.ts, src/media/live-view-service.ts, src/config/camera-config.ts, src/camera/reolink-session.ts, .planning/phases/02-browser-live-view-pipeline/02-CONTEXT.md, .planning/phases/02-browser-live-view-pipeline/02-UI-SPEC.md</read_first>
  <action>Create `src/server/create-server.ts` that builds a Fastify app, serves `web/dist` through `@fastify/static`, and mounts a route module from `src/server/routes/live-view.ts`. In `src/server/routes/live-view.ts`, add exact routes `GET /api/live-view`, `GET /api/live-view/snapshot/:quality`, and `GET /api/live-view/health`. `GET /api/live-view` must return the `LiveViewBootstrap` JSON from `buildLiveViewBootstrap(request.headers.host ?? "127.0.0.1:4000")`, including each mode's `playback.whepUrl`, `playback.hlsUrl`, and `playback.snapshotUrl` fields, and it must never include `baseUrl`, `username`, `password`, `token=`, or raw RTSP camera URLs. `GET /api/live-view/snapshot/:quality` must accept only `main` or `sub`, return `image/jpeg`, set `Cache-Control: no-store`, and proxy the camera snapshot server-side so credentials never reach the browser. `GET /api/live-view/health` must return a short JSON object with `relay: "starting" | "ready" | "failed"` and a `reason` string when unavailable. Update `src/index.ts` so `node --import tsx src/index.ts probe` keeps the Phase 1 probe path, while the default command starts the Fastify server on `process.env.PORT ?? 4000` and starts the MediaMTX relay. Create `tests/server/live-view-routes.test.ts` that asserts `/api/live-view` returns `preferredModeId`, `fallbackOrder`, `modes`, and browser-safe playback endpoint fields, asserts the serialized response does not contain `token=`, `rtsp://`, or the configured password, and asserts snapshot validation rejects an invalid quality.</action>
  <verify>
    <automated>npx vitest run tests/server/live-view-routes.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - src/server/routes/live-view.ts contains `/api/live-view`
    - src/server/routes/live-view.ts contains `snapshot/:quality`
    - src/server/routes/live-view.ts contains `Cache-Control`
    - src/server/routes/live-view.ts contains `/api/live-view/health`
    - src/index.ts contains `process.env.PORT ?? 4000`
    - src/index.ts contains `probe`
    - tests/server/live-view-routes.test.ts contains `preferredModeId`
    - tests/server/live-view-routes.test.ts contains `whepUrl`
    - tests/server/live-view-routes.test.ts contains `not.toContain("token=")`
    - tests/server/live-view-routes.test.ts contains `not.toContain("rtsp://")`
  </acceptance_criteria>
  <done>The Node app owns the browser-facing live-view API and exposes clear startup/health data without leaking camera secrets.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build` succeeds
- [ ] `npx vitest run tests/media/mediamtx-runtime-config.test.ts -x` succeeds
- [ ] `npx vitest run tests/media/live-view-errors.test.ts -x` succeeds
- [ ] `npx vitest run tests/server/live-view-routes.test.ts -x` succeeds
- [ ] Starting the default `src/index.ts` command launches Fastify and the MediaMTX sidecar while `probe` still works
</verification>

<success_criteria>
- MediaMTX is pinned and bootstrapped locally without requiring Docker
- The app returns browser-safe WHEP, HLS, and snapshot URLs plus fallback metadata from `/api/live-view`
- Snapshot fallback and relay health are exposed through app-owned routes with short reasons for failure
</success_criteria>

<output>
After completion, create `.planning/phases/02-browser-live-view-pipeline/02-02-SUMMARY.md`
</output>
