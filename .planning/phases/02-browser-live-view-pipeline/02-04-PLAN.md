---
phase: 02-browser-live-view-pipeline
plan: 04
type: execute
wave: 3
depends_on:
  - "02-03"
files_modified:
  - web/src/main.tsx
  - web/src/App.tsx
  - web/src/styles.css
  - web/src/components/LiveViewerFrame.tsx
  - web/src/components/ViewerStatusOverlay.tsx
  - web/src/components/ModeSwitcher.tsx
  - web/src/components/DiagnosticsDisclosure.tsx
  - tests/web/live-view-controls.test.tsx
autonomous: true
requirements:
  - LIVE-02
  - LIVE-03
user_setup: []
must_haves:
  truths:
    - "The user can switch between supported stream qualities and fallback modes after startup"
    - "The viewer shows connecting, live, reconnecting, and failed states in-frame with retry and short reasons"
    - "Diagnostics stay secondary while still exposing current mode, last reason, and next fallback"
  artifacts:
    - path: "web/src/components/LiveViewerFrame.tsx"
      provides: "Primary viewer surface and overlay composition"
      contains: "Retry Live View"
    - path: "web/src/components/ModeSwitcher.tsx"
      provides: "Manual transport and quality selector"
      contains: "WebRTC Main"
    - path: "tests/web/live-view-controls.test.tsx"
      provides: "UI verification for manual controls and diagnostics disclosure"
      contains: "Diagnostics"
  key_links:
    - from: "web/src/App.tsx"
      to: "web/src/hooks/use-live-view.ts"
      via: "viewer UI composed around the shared state-machine hook"
      pattern: "useLiveView"
    - from: "web/src/components/ModeSwitcher.tsx"
      to: "web/src/hooks/use-live-view.ts"
      via: "manual mode selection stays sticky until definitive failure"
      pattern: "selectMode"
---

<objective>
Build the React viewer shell, manual mode controls, and diagnostics surfaces around the transport/state layer from 02-03.

Purpose: Turn the underlying playback and retry logic into the viewer-first browser experience approved in the Phase 2 UI contract.
Output: React entry app, viewer frame and overlays, mode switcher, diagnostics disclosure, app styling, and jsdom coverage for the interactive controls.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-browser-live-view-pipeline/02-CONTEXT.md
@.planning/phases/02-browser-live-view-pipeline/02-RESEARCH.md
@.planning/phases/02-browser-live-view-pipeline/02-UI-SPEC.md
@.planning/phases/02-browser-live-view-pipeline/02-VALIDATION.md
@.planning/phases/02-browser-live-view-pipeline/02-03-SUMMARY.md
@src/types/live-view.ts

<interfaces>
From web/src/hooks/use-live-view.ts (created in 02-03):
```ts
export function useLiveView(): {
  state: { kind: ViewerStateKind; reason?: string };
  modes: LiveMode[];
  currentModeId: LiveModeId | null;
  selectMode(modeId: LiveModeId): Promise<void>;
  retry(): Promise<void>;
};
```

The UI must keep these exact labels from 02-UI-SPEC:
```text
Connecting
Live
Reconnecting
Live View Failed
Retry Live View
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the viewer shell, status overlay, and layout contract</name>
  <files>web/src/main.tsx, web/src/App.tsx, web/src/styles.css, web/src/components/LiveViewerFrame.tsx, web/src/components/ViewerStatusOverlay.tsx</files>
  <read_first>web/src/main.tsx, web/src/App.tsx, web/src/styles.css, web/src/components/LiveViewerFrame.tsx, web/src/components/ViewerStatusOverlay.tsx, web/src/hooks/use-live-view.ts, .planning/phases/02-browser-live-view-pipeline/02-UI-SPEC.md, src/types/live-view.ts</read_first>
  <action>Create `web/src/main.tsx` and `web/src/App.tsx` to render a single viewer-first dashboard around `useLiveView()`. Implement `LiveViewerFrame` and `ViewerStatusOverlay` using the approved UI contract: IBM Plex Sans, dominant background `#12161B`, secondary surfaces `#1D242C`, accent `#63C7B2`, 12px radius, and a viewer frame that occupies at least `70vh` on desktop. Overlay labels must be exact text `Connecting`, `Live`, `Reconnecting`, and `Live View Failed`, with a short reason line under 90 characters and a polite live region that mirrors the visible overlay text. Keep the layout limited to Phase 2 scope: viewer first, no PTZ or settings chrome.</action>
  <verify>
    <automated>npm run build:web</automated>
  </verify>
  <done>The browser app renders the approved viewer shell and in-frame state overlays around the shared hook.</done>
</task>

<task type="auto">
  <name>Task 2: Add the manual mode switcher, diagnostics disclosure, and UI interaction tests</name>
  <files>web/src/App.tsx, web/src/components/ModeSwitcher.tsx, web/src/components/DiagnosticsDisclosure.tsx, tests/web/live-view-controls.test.tsx</files>
  <read_first>web/src/App.tsx, web/src/components/ModeSwitcher.tsx, web/src/components/DiagnosticsDisclosure.tsx, tests/web/live-view-controls.test.tsx, web/src/hooks/use-live-view.ts, .planning/phases/02-browser-live-view-pipeline/02-UI-SPEC.md, src/types/live-view.ts</read_first>
  <action>Implement `ModeSwitcher` and `DiagnosticsDisclosure` using the approved interaction contract. `ModeSwitcher` labels must separate transport and quality, for example `WebRTC Main`, `HLS Sub`, and `Snapshot Main`, per D-04, and the segmented buttons must keep a `44px` minimum hit area. `DiagnosticsDisclosure` must stay collapsed by default and show current mode, last reason, and next fallback mode without exposing raw credentials or camera URLs. Update `web/src/App.tsx` to wire these components to `useLiveView()` and preserve the sticky manual mode behavior implemented in 02-03. Create `tests/web/live-view-controls.test.tsx` to verify manual mode switching, diagnostics disclosure toggling, the exact `Retry Live View` label, and the presence of all required overlay labels.</action>
  <verify>
    <automated>npx vitest run tests/web/live-view-controls.test.tsx -x</automated>
  </verify>
  <done>The browser UI exposes manual switching and diagnostics without violating the approved viewer-first layout.</done>
</task>

</tasks>

<verification>
- [ ] `npm run build:web` succeeds
- [ ] `npx vitest run tests/web/live-view-controls.test.tsx -x` succeeds
- [ ] The viewer shows the exact approved overlay labels and exposes manual switching plus diagnostics disclosure
</verification>

<success_criteria>
- The browser UI matches the approved Phase 2 contract
- Manual quality and fallback switching is visible and test-covered
- Diagnostics remain secondary while the primary viewer keeps retry and state feedback in-frame
</success_criteria>

<output>
After completion, create `.planning/phases/02-browser-live-view-pipeline/02-04-SUMMARY.md`
</output>
