---
phase: 01-camera-connectivity-capability-map
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - src/config/camera-config.ts
  - src/types/reolink.ts
  - tests/fixtures/reolink/.gitkeep
autonomous: true
requirements:
  - CONN-01
user_setup: []
must_haves:
  truths:
    - "User can define the target camera's LAN connection details in a first-run local config file"
    - "Project has a typed Node.js foundation for later camera adapter work"
  artifacts:
    - path: "package.json"
      provides: "Node.js project scripts and dependencies"
      contains: "\"name\": \"reolink\""
    - path: "src/config/camera-config.ts"
      provides: "Config schema and persistence"
      contains: "export type CameraConfig"
    - path: "src/types/reolink.ts"
      provides: "Shared camera payload types"
      contains: "export type ReolinkCommand"
  key_links:
    - from: "src/config/camera-config.ts"
      to: "src/types/reolink.ts"
      via: "shared camera identity and config typing"
      pattern: "Reolink"
---

<objective>
Establish the local Node/TypeScript project foundation and first-run config path for the RLC-423S integration work.

Purpose: Give later plans a stable runtime, shared types, and a persisted local config format that matches the Phase 1 decisions.
Output: Bootstrapped project files, typed config loader/persistence logic, and fixture directory scaffolding.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-camera-connectivity-capability-map/01-CONTEXT.md
@.planning/phases/01-camera-connectivity-capability-map/01-RESEARCH.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize the Node.js and TypeScript workspace</name>
  <files>package.json, tsconfig.json</files>
  <read_first>.planning/PROJECT.md, .planning/phases/01-camera-connectivity-capability-map/01-CONTEXT.md, .planning/phases/01-camera-connectivity-capability-map/01-RESEARCH.md, AGENTS.md</read_first>
  <action>Create `package.json` for a local Node.js TypeScript project with `"name": "reolink"`, `"private": true`, `"type": "module"`, and scripts `"build": "tsc -p tsconfig.json"`, `"test": "vitest run"`, and `"probe": "node --import tsx src/index.ts"`. Add runtime dependencies `zod` and `pino`, and dev dependencies `typescript`, `tsx`, `vitest`, and `@types/node`. Create `tsconfig.json` with `target: "ES2022"`, `module: "NodeNext"`, `moduleResolution: "NodeNext"`, `rootDir: "."`, `outDir: "dist"`, `strict: true`, `esModuleInterop: true`, `resolveJsonModule: true`, and include `src/**/*.ts` plus `tests/**/*.ts`.</action>
  <verify>findstr /c:"\"name\": \"reolink\"" package.json && findstr /c:"\"probe\": \"node --import tsx src/index.ts\"" package.json && findstr /c:"\"module\": \"NodeNext\"" tsconfig.json</verify>
  <acceptance_criteria>
    - package.json contains `"name": "reolink"`
    - package.json contains `"build": "tsc -p tsconfig.json"`
    - package.json contains `"test": "vitest run"`
    - tsconfig.json contains `"module": "NodeNext"`
    - tsconfig.json contains `"strict": true`
  </acceptance_criteria>
  <done>Project can install dependencies and has the exact scripts and TypeScript options needed by later plans.</done>
</task>

<task type="auto">
  <name>Task 2: Implement typed local camera config persistence</name>
  <files>src/config/camera-config.ts, src/types/reolink.ts</files>
  <read_first>.planning/phases/01-camera-connectivity-capability-map/01-CONTEXT.md, .planning/phases/01-camera-connectivity-capability-map/01-RESEARCH.md, camera_stats.txt, package.json, tsconfig.json</read_first>
  <action>Create `src/types/reolink.ts` with shared types including `ReolinkCommand`, `CameraIdentity`, and `CapabilityPermit`. Create `src/config/camera-config.ts` exporting `CameraConfig`, a Zod schema named `cameraConfigSchema`, `defaultConfigPath()`, `loadCameraConfig()`, and `saveCameraConfig()`. The persisted config shape must include `baseUrl`, `username`, `password`, `modelHint`, `notes`, and a nested `snapshot` object with `model`, `hardVer`, and `firmVer` fields reserved for future updates. Store the config under a deterministic local path inside the project, not environment variables.</action>
  <verify>findstr /c:"export type CameraConfig" src\\config\\camera-config.ts && findstr /c:"cameraConfigSchema" src\\config\\camera-config.ts && findstr /c:"export type ReolinkCommand" src\\types\\reolink.ts</verify>
  <acceptance_criteria>
    - src/config/camera-config.ts contains `export type CameraConfig`
    - src/config/camera-config.ts contains `cameraConfigSchema`
    - src/config/camera-config.ts contains `export async function loadCameraConfig`
    - src/config/camera-config.ts contains `export async function saveCameraConfig`
    - src/types/reolink.ts contains `export type ReolinkCommand`
  </acceptance_criteria>
  <done>The codebase has a concrete local config API aligned with the first-run config-file decision.</done>
</task>

<task type="auto">
  <name>Task 3: Seed the fixture and test scaffold for camera discovery work</name>
  <files>tests/fixtures/reolink/.gitkeep, package.json</files>
  <read_first>.planning/phases/01-camera-connectivity-capability-map/01-VALIDATION.md, package.json, tsconfig.json</read_first>
  <action>Create `tests/fixtures/reolink/.gitkeep` and ensure the workspace layout anticipates fixture-backed tests from the live camera responses captured in planning. If `package.json` lacks a `test` script or Vitest dependency after Task 1, fix it here so later plans can immediately add adapter tests without revisiting the foundation.</action>
  <verify>if exist tests\\fixtures\\reolink\\.gitkeep (echo ok) else (exit /b 1)</verify>
  <acceptance_criteria>
    - tests/fixtures/reolink/.gitkeep exists
    - package.json contains `"test": "vitest run"`
  </acceptance_criteria>
  <done>The repository is ready to accept captured Login/GetDevInfo/GetAbility/GetNetPort fixtures in later execution steps.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm install` succeeds
- [ ] `npm run build` succeeds
- [ ] `npm test` runs without a missing-script error
</verification>

<success_criteria>

- All tasks completed
- The project has a concrete local config path instead of environment-variable-only setup
- Shared types exist for later adapter work
- Test fixture scaffolding exists for live camera payloads
  </success_criteria>

<output>
After completion, create `.planning/phases/01-camera-connectivity-capability-map/01-01-SUMMARY.md`
</output>
