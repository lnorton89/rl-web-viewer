---
phase: 01-camera-connectivity-capability-map
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - src/camera/capability-snapshot.ts
  - src/diagnostics/debug-capture.ts
  - src/index.ts
  - tests/capability-snapshot.test.ts
  - tests/debug-capture.test.ts
autonomous: true
requirements:
  - CONN-03
  - COMP-01
  - COMP-02
  - COMP-03
user_setup: []
must_haves:
  truths:
    - "The app derives a stored capability snapshot from the real camera/firmware rather than only model-name assumptions"
    - "Unsupported features can be hidden or gated from later UI work through normalized capability flags"
    - "Debug capture is opt-in and produces useful sanitized artifacts for troubleshooting"
  artifacts:
    - path: "src/camera/capability-snapshot.ts"
      provides: "Capability normalization and persistence"
      contains: "export function buildCapabilitySnapshot"
    - path: "src/diagnostics/debug-capture.ts"
      provides: "Opt-in request and response artifact writer"
      contains: "export async function writeDebugArtifact"
    - path: "src/index.ts"
      provides: "Local probe entry point or smoke command"
      contains: "probe"
  key_links:
    - from: "src/camera/capability-snapshot.ts"
      to: "src/camera/reolink-discovery.ts"
      via: "normalized snapshot built from raw discovery payloads"
      pattern: "getAbility|probeCamera"
    - from: "src/index.ts"
      to: "src/diagnostics/debug-capture.ts"
      via: "debug mode toggle and artifact emission"
      pattern: "debug"
---

<objective>
Normalize the raw discovery output into a stored capability snapshot and implement the opt-in debug artifact path that Phase 1 promised.

Purpose: Finish the adapter boundary so later phases can rely on clean capability flags and actionable troubleshooting data.
Output: Capability snapshot builder/persistence, sanitized debug artifact writer, and a local probe command that runs without cloud dependencies.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-camera-connectivity-capability-map/01-CONTEXT.md
@.planning/phases/01-camera-connectivity-capability-map/01-RESEARCH.md
@.planning/phases/01-camera-connectivity-capability-map/01-VALIDATION.md
@.planning/phases/01-camera-connectivity-capability-map/01-02-SUMMARY.md
@camera_stats.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize and persist a firmware-aware capability snapshot</name>
  <files>src/camera/capability-snapshot.ts, tests/capability-snapshot.test.ts</files>
  <read_first>.planning/phases/01-camera-connectivity-capability-map/01-CONTEXT.md, .planning/phases/01-camera-connectivity-capability-map/01-RESEARCH.md, .planning/phases/01-camera-connectivity-capability-map/01-02-SUMMARY.md, src/camera/reolink-discovery.ts, src/types/reolink.ts, src/config/camera-config.ts</read_first>
  <action>Create `src/camera/capability-snapshot.ts` exporting `buildCapabilitySnapshot()` and `saveCapabilitySnapshot()`. The normalized snapshot must include identity (`model`, `hardVer`, `firmVer`), ports (`http`, `https`, `media`, `onvif`, `rtsp`), and app-level booleans or enums for at least `supportsLiveView`, `supportsPtzControl`, `supportsPtzPreset`, `supportsPtzPatrol`, `supportsSnapshot`, and `supportsConfigRead`. Persist the snapshot to a local project file path derived from the configured camera base URL or identity so later phases can reuse it without reparsing raw ability payloads. Add `tests/capability-snapshot.test.ts` that verifies the RLC-423S fixture normalizes `live`, `ptzCtrl`, and `ptzPreset` into expected app-level flags.</action>
  <verify>findstr /c:"export function buildCapabilitySnapshot" src\\camera\\capability-snapshot.ts && findstr /c:"supportsPtzControl" src\\camera\\capability-snapshot.ts && findstr /c:"supportsLiveView" tests\\capability-snapshot.test.ts</verify>
  <acceptance_criteria>
    - src/camera/capability-snapshot.ts contains `export function buildCapabilitySnapshot`
    - src/camera/capability-snapshot.ts contains `supportsPtzControl`
    - src/camera/capability-snapshot.ts contains `supportsLiveView`
    - tests/capability-snapshot.test.ts contains `supportsPtzPreset`
  </acceptance_criteria>
  <done>The codebase has a capability snapshot format later UI and feature layers can consume directly.</done>
</task>

<task type="auto">
  <name>Task 2: Implement opt-in debug capture with redaction</name>
  <files>src/diagnostics/debug-capture.ts, tests/debug-capture.test.ts</files>
  <read_first>.planning/phases/01-camera-connectivity-capability-map/01-CONTEXT.md, .planning/phases/01-camera-connectivity-capability-map/01-RESEARCH.md, .planning/phases/01-camera-connectivity-capability-map/01-VALIDATION.md, src/camera/reolink-session.ts, src/camera/reolink-discovery.ts</read_first>
  <action>Create `src/diagnostics/debug-capture.ts` exporting `writeDebugArtifact()` and `sanitizeForDebug()`. The artifact format must capture timestamp, command name, URL or endpoint path, HTTP status, and parsed response body, while redacting password fields and masking token values. Add `tests/debug-capture.test.ts` to prove a sample Login/debug payload does not write the raw blank-password field name/value pair or the full token string unchanged.</action>
  <verify>findstr /c:"export async function writeDebugArtifact" src\\diagnostics\\debug-capture.ts && findstr /c:"sanitizeForDebug" src\\diagnostics\\debug-capture.ts && findstr /c:"token" tests\\debug-capture.test.ts</verify>
  <acceptance_criteria>
    - src/diagnostics/debug-capture.ts contains `export async function writeDebugArtifact`
    - src/diagnostics/debug-capture.ts contains `sanitizeForDebug`
    - tests/debug-capture.test.ts contains `password`
    - tests/debug-capture.test.ts contains `token`
  </acceptance_criteria>
  <done>Phase 1 troubleshooting artifacts are useful without turning debug mode into a credential leak.</done>
</task>

<task type="auto">
  <name>Task 3: Expose a local, no-cloud probe entry point for Phase 1 smoke checks</name>
  <files>src/index.ts, src/camera/capability-snapshot.ts, src/diagnostics/debug-capture.ts</files>
  <read_first>.planning/phases/01-camera-connectivity-capability-map/01-CONTEXT.md, .planning/phases/01-camera-connectivity-capability-map/01-RESEARCH.md, .planning/phases/01-camera-connectivity-capability-map/01-02-SUMMARY.md, package.json, src/config/camera-config.ts, src/camera/reolink-discovery.ts</read_first>
  <action>Create `src/index.ts` as the initial probe entry point invoked by the `probe` script. The command should load local config, run the discovery flow, build the capability snapshot, print a concise local-only success summary containing model, firmware, and key capability flags, and optionally emit debug artifacts only when a debug toggle in config or CLI args is enabled. Do not introduce any Reolink cloud, UID, or P2P dependency paths in this entry point.</action>
  <verify>findstr /c:"probe" src\\index.ts && findstr /c:"firmVer" src\\index.ts && findstr /c:"supportsPtzControl" src\\index.ts</verify>
  <acceptance_criteria>
    - src/index.ts contains `probe`
    - src/index.ts contains `firmVer`
    - src/index.ts contains `supportsPtzControl`
    - src/index.ts does not contain `uid`
  </acceptance_criteria>
  <done>The project has a Phase 1 smoke path that proves local-only discovery and capability storage end to end.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] `npm test` covers capability snapshot and debug capture modules
- [ ] `npm run probe` completes against the local config without requiring cloud/UID setup
</verification>

<success_criteria>

- All tasks completed
- Capability normalization exists as a stable adapter boundary
- Debug capture is opt-in and sanitized
- The local probe command proves Phase 1 can connect and discover without cloud access
  </success_criteria>

<output>
After completion, create `.planning/phases/01-camera-connectivity-capability-map/01-03-SUMMARY.md`
</output>
