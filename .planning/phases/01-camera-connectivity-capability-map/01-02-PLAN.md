---
phase: 01-camera-connectivity-capability-map
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/camera/reolink-session.ts
  - src/camera/reolink-discovery.ts
  - tests/reolink-session.test.ts
  - tests/reolink-discovery.test.ts
autonomous: true
requirements:
  - CONN-02
  - CONN-04
user_setup: []
must_haves:
  truths:
    - "User can validate connectivity and see real device identity from the RLC-423S"
    - "Auth/session handling can recover from token expiry without a manual restart path"
  artifacts:
    - path: "src/camera/reolink-session.ts"
      provides: "Token login and authenticated request execution"
      contains: "export class ReolinkSession"
    - path: "src/camera/reolink-discovery.ts"
      provides: "Safe read-only probes for device and port discovery"
      contains: "GetAbility"
    - path: "tests/reolink-session.test.ts"
      provides: "Session manager verification"
    - path: "tests/reolink-discovery.test.ts"
      provides: "Discovery probe verification"
  key_links:
    - from: "src/camera/reolink-session.ts"
      to: "src/camera/reolink-discovery.ts"
      via: "authenticated request executor"
      pattern: "requestJson"
---

<objective>
Implement the authenticated request/session layer and the first safe discovery probes against the Reolink CGI API.

Purpose: Turn the planning-time live probes into reusable code that later phases can trust.
Output: Session manager, discovery helpers for device info and ports/capabilities, and fixture-backed tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-camera-connectivity-capability-map/01-CONTEXT.md
@.planning/phases/01-camera-connectivity-capability-map/01-RESEARCH.md
@.planning/phases/01-camera-connectivity-capability-map/01-VALIDATION.md
@.planning/phases/01-camera-connectivity-capability-map/01-01-SUMMARY.md
@camera_stats.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the token-authenticated Reolink session manager</name>
  <files>src/camera/reolink-session.ts, tests/reolink-session.test.ts</files>
  <read_first>.planning/phases/01-camera-connectivity-capability-map/01-CONTEXT.md, .planning/phases/01-camera-connectivity-capability-map/01-RESEARCH.md, .planning/phases/01-camera-connectivity-capability-map/01-01-SUMMARY.md, src/config/camera-config.ts, src/types/reolink.ts, package.json</read_first>
  <action>Create `src/camera/reolink-session.ts` exporting `ReolinkSession`, `login()`, `requestJson()`, and token-expiry helpers. The login payload must use `[{\"cmd\":\"Login\",\"action\":0,\"param\":{\"User\":{\"userName\":...,\"password\":...}}}]`, parse `Token.name` and `Token.leaseTime`, and cache an `expiresAt` value. `requestJson()` must POST to `/cgi-bin/api.cgi?token=...` after login, retry once after an auth-related failure by forcing re-login, and never log the raw password. Create `tests/reolink-session.test.ts` covering token parsing, cached token reuse, and forced re-login when auth state is invalidated.</action>
  <verify>findstr /c:"export class ReolinkSession" src\\camera\\reolink-session.ts && findstr /c:"leaseTime" src\\camera\\reolink-session.ts && findstr /c:"retry" tests\\reolink-session.test.ts</verify>
  <acceptance_criteria>
    - src/camera/reolink-session.ts contains `export class ReolinkSession`
    - src/camera/reolink-session.ts contains `Token`
    - src/camera/reolink-session.ts contains `leaseTime`
    - tests/reolink-session.test.ts contains `re-login` or `relogin`
  </acceptance_criteria>
  <done>The codebase has one shared, retry-capable token auth path instead of ad hoc request logic.</done>
</task>

<task type="auto">
  <name>Task 2: Implement safe read-only discovery probes</name>
  <files>src/camera/reolink-discovery.ts, tests/reolink-discovery.test.ts</files>
  <read_first>.planning/phases/01-camera-connectivity-capability-map/01-RESEARCH.md, .planning/phases/01-camera-connectivity-capability-map/01-01-SUMMARY.md, src/camera/reolink-session.ts, src/types/reolink.ts, camera_stats.txt</read_first>
  <action>Create `src/camera/reolink-discovery.ts` with explicit functions `getDevInfo()`, `getNetPort()`, and `getAbility()`, plus a combined `probeCamera()` helper that returns identity, ports, and raw ability payloads. The implementation must use only read-only commands observed during planning and preserve the actual RLC-423S values for model `RLC-423S`, hardware `IPC_3816M`, and firmware `v2.0.0.1055_17110905_v1.0.0.30` in tests/fixtures. Create `tests/reolink-discovery.test.ts` that verifies parsing for model, hardware, firmware, HTTP/HTTPS/RTSP/ONVIF/media ports, and the existence of `live`, `ptzCtrl`, and `ptzPreset` keys in the raw ability response.</action>
  <verify>findstr /c:"getDevInfo" src\\camera\\reolink-discovery.ts && findstr /c:"getAbility" src\\camera\\reolink-discovery.ts && findstr /c:"RLC-423S" tests\\reolink-discovery.test.ts</verify>
  <acceptance_criteria>
    - src/camera/reolink-discovery.ts contains `getDevInfo`
    - src/camera/reolink-discovery.ts contains `getNetPort`
    - src/camera/reolink-discovery.ts contains `getAbility`
    - tests/reolink-discovery.test.ts contains `RLC-423S`
    - tests/reolink-discovery.test.ts contains `ptzCtrl`
  </acceptance_criteria>
  <done>The discovery layer can prove connectivity and extract the exact device identity and key port/capability information.</done>
</task>

<task type="auto">
  <name>Task 3: Store the known-good camera payload fixtures used by the discovery tests</name>
  <files>tests/reolink-session.test.ts, tests/reolink-discovery.test.ts, tests/fixtures/reolink/login.json, tests/fixtures/reolink/get-dev-info.json, tests/fixtures/reolink/get-net-port.json, tests/fixtures/reolink/get-ability.json</files>
  <read_first>.planning/phases/01-camera-connectivity-capability-map/01-RESEARCH.md, .planning/phases/01-camera-connectivity-capability-map/01-VALIDATION.md, tests/fixtures/reolink/.gitkeep</read_first>
  <action>Create fixture files from the observed planning-time payloads: `login.json`, `get-dev-info.json`, `get-net-port.json`, and `get-ability.json`. Update the session and discovery tests to load those fixtures instead of inlining large payload objects. Keep the fixture files sanitized so they do not contain live token values or embedded credentials beyond the known blank-password login shape required to model the camera behavior.</action>
  <verify>if exist tests\\fixtures\\reolink\\login.json (echo ok) else (exit /b 1) && if exist tests\\fixtures\\reolink\\get-ability.json (echo ok) else (exit /b 1)</verify>
  <acceptance_criteria>
    - tests/fixtures/reolink/login.json exists
    - tests/fixtures/reolink/get-dev-info.json exists
    - tests/fixtures/reolink/get-net-port.json exists
    - tests/fixtures/reolink/get-ability.json exists
  </acceptance_criteria>
  <done>The core probes are backed by reproducible fixtures, not only live-device assumptions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds
- [ ] `npm test` covers session and discovery modules
- [ ] The parsed device identity matches `RLC-423S`, `IPC_3816M`, and `v2.0.0.1055_17110905_v1.0.0.30`
</verification>

<success_criteria>

- All tasks completed
- Connectivity validation and discovery probes are implemented with fixture-backed tests
- Token expiry handling exists in the shared request path
- Discovery is grounded in the actual target firmware responses
  </success_criteria>

<output>
After completion, create `.planning/phases/01-camera-connectivity-capability-map/01-02-SUMMARY.md`
</output>
